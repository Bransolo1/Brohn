# SC-IAT, camera analysis and participant preservation checkpoint

Prepared 24 September 2026 for the owner's requested pause and publication to
`Bransolo1/Brohn` on `main`. Development is paused; the
[workstream](../WORKSTREAM.md) records the next steps. This is not a whole-platform release or a
scientific qualification claim.

## Connected changes

- The sixth registered task is the explicitly named Brohn response-window
  SC-IAT. Its 192-trial procedure, material roles, allocation, original key
  journal, replay, scoring, summary import and cohort integration are separate
  from the five existing task profiles. The scientific procedure and its
  differences from other SC-IAT implementations are documented in
  [candidate evidence](SCIAT-WINDOW-CANDIDATE.md) and
  [core acceptance](SCIAT-WINDOW-CORE-ACCEPTANCE.md).
- Optional local video analysis now reports 20 facial action-unit outputs and
  seven model-native category scores using pinned PyFeat v1 models and runtime
  bytes. Exact windows, explicit stride, no-face/multiple-face support,
  complete exports and unchanged reopening are covered by
  [facial acceptance](FACIAL-EXPRESSION-ACCEPTANCE.md). These outputs do not
  establish feelings, attention, liking or identity.
- Local setup, saved configuration, launch checks and doctor support the
  optional facial environment and external model/shared-FFmpeg directories.
  [Runtime acceptance](FACIAL-RUNTIME-ACCEPTANCE.md) records the exact installed
  Windows profile and tested failure recovery.
- Canonical event encoding preserves the original bytes and hashes while
  reducing the tested 3.1 MB envelope encoding time. The complete receiver
  still replays retained history; see
  [encoder evidence](CANONICAL-ENCODER-PERFORMANCE.md) and
  [upload-budget review](PARTICIPANT-UPLOAD-BUDGET-REVIEW.md).

## Evidence boundaries

The facial journey has 16 browser groups **including** five clean accessibility
scans, four actual jobs, exact native/CSV/HTML exports and restart. The separate
native reference case, R adapter, source guards and configuration receipts are
listed in its acceptance documents. A later mapping-status wording correction
has four separate targeted checks and does not invent another inference run.

The SC-IAT connected journey and its omitted/fast-response serialization repair
are recorded in [researcher acceptance](SCIAT-WINDOW-RESEARCHER-ACCEPTANCE.md).
Four full administrations, eight native/imported reports and a reviewed cohort
now pass exact source/export/restart checks; 102 independent checks audit raw keys
and Fraction-based scoring across 13 successful jobs. The final chart-label
continuation preserves complete values and geometry and covers the five earlier
profiles, including BIAT within-block warm-up exclusions. That acceptance
document remains authoritative about completed and pending checks; a
component arithmetic test alone is not evidence that a saved report succeeds.
Original failed harness attempts and original participant evidence are retained.

## Camera, delivery and provenance acceptance

The new camera policy/permission and preserved participant-code modules are now
loaded and their production hooks are connected. Participant-code preservation
passes 57 direct checks, with publication, atomic admission and backup passing
27 connected dispatcher/store checks. The separate actual browser journey passes
74 checks/three scans. A focused final-runner follow-up passes 20 checks/three
scans for failed local persistence, uncertain starts and recovery of an already
admitted session without a duplicate start. See [runtime acceptance](PARTICIPANT-RUNTIME-BROWSER-ACCEPTANCE.md).

Researcher provenance passes 30 domain/Shiny checks, seven connected checks/four
scans and 21 independent source/export checks. The final narrow-screen wrapping
correction has two browser checks/two scans and four independent checks. One
native response report remains unchanged through review, downloads, collection
closure, History and restart; those operations create zero additional jobs.
See [provenance acceptance](PARTICIPANT-CODE-PROVENANCE-ACCEPTANCE.md).

Automatic camera analysis passes 13 loaded integration checks and the actual
browser-to-report journey: 17 joined groups including seven clean scans, five
successful jobs with exactly one platform facial inference, and six independent
native-reference checks against the exact encoded browser recording. Complete
CSV/JSONL, original named permission, partial/interrupted source and unchanged
restart retain their separate evidence. The camera feed is explicitly synthetic;
this does not establish model accuracy or physical stimulus synchronization.
See [camera acceptance](CAMERA-FACIAL-AUTOMATION-ACCEPTANCE.md). Their
integration contracts are
[camera analysis](../architecture/CAMERA-ANALYSIS-CONTRACT.md) and
[participant runtime preservation](../architecture/PARTICIPANT-RUNTIME-PRESERVATION.md).
Preferred batching is now in production and passes 31 helper checks, 31 actual
browser checks and two clean narrow-screen scans. Original pending operations
retain their exact bytes, and both saved journals retain all 394 events and
15,000 key observations. See [batching acceptance](PARTICIPANT-PREFERRED-BATCH-ACCEPTANCE.md)
for measured per-request timings and the remaining accumulated-history limit.

Public source includes implementation, reproduction scripts, architecture and
handoff documentation. Participant stores, test media, downloaded model weights,
native binaries and large local receipts remain outside Git. See
[current status](../../STATUS.md), [John's handoff](../HANDOFF-JOHN.md) and the
[integrated workflow ledger](INTEGRATED-WORKFLOWS-20260924.md) before continuing.

## Pause and publication checks

All owned researcher, participant, worker and QA browser/native processes are
stopped. The next proposed workstreams were investigated read-only; no new
portable smoke test, facial temporal explorer or linked-media feature was begun.

The final source audit at
`work/test-runs/brohn-pause-publication-20260924-01` checks 155 changed/new files,
12 Python files, five JSON files, 26 JavaScript files, five PowerShell scripts,
75 R files and 342 relative documentation links. The full R/Shiny application
loader succeeds, all 143 QA catalog entries reference existing unique tests,
and the candidate set contains no detected credentials, binary runtime/model
assets, research stores or files above its 2 MiB inspection limit. Git whitespace
checks pass. These are source-publication checks, not another full method suite
or clean-machine installation. The initial multiline R command produced no
receipt and was not counted; the explicit script invocation produced the saved
passing syntax/loader receipt.

The staged-file check also found trailing whitespace or an extra blank final
line in five new test harnesses. Only that formatting was removed; all original
test-source bytes and their hashes were retained externally, and the four
affected JavaScript files plus the camera R test parsed again. The accepted
browser/native receipts retain their original source identities; this formatting
cleanup is not presented as another executed journey.
