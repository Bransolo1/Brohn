# Saved task source responsiveness

Status: qualified external candidate; the parent subsequently installed the exact five files. Production files and Git were not changed by this agent. The browser used two isolated copies of the then-frozen application; the candidate overlays exactly three R modules and one new JavaScript asset. A fifth file updates the existing task-plot test for the explicit preparation acknowledgement. The later combined production phase, including the separate media feedback change, is distinct from this browser receipt.

## Behaviour and retained guarantees

Opening task plots now presents a live preparation message before reading the complete saved source. A fresh ticket identifies this request. The browser observes that exact visible element across two animation frames, then acknowledges the ticket. R ignores stale acknowledgements and rechecks the current report, project, selected source and control identity before reading. Navigation clears pending work. Success and failure have explicit states. This is still synchronous work after acknowledgement; it does not claim background execution or cancellation.

The three modules reuse the existing `.brohn_sv_hash` helper for canonical R-value digests. Its 128-entry memo stores digest pairs only, keyed by a SHA-256 fingerprint of the entire current serialized R value. It retains no record bodies, permissions, file-verification decisions or replay results. The global `brohn_hash` implementation is unchanged. Every source read still reads and replays the complete original journal, validates the frozen task registry, verifies saved provenance and performs current source/access checks. Downloads still reload their complete source and compare its identity.

The actual source is the original synthetic browser-collected GNAT002 report: 384 trials, 789 durable outer journal events. No study was administered again, no report was recalculated, and the actual-store/browser qualification created zero scientific jobs. The 786-event assumption in an early harness was incorrect for this browser source and is retained as a failed attempt.

## Measured results

These are sequential local observations over one retained source, not a capacity or concurrency benchmark.

| Measurement | Existing implementation | Candidate |
| --- | ---: | ---: |
| Matched R first complete source | 12.75 s | 10.04 s |
| Matched R repeated complete source | 11.23 s | 5.50 s |
| Actual browser first complete source | 15.11 s | 13.10 s |
| Actual browser repeated source | 15.82 s, after explicit catalog reopen and cleared old model | 8.27 s |
| Candidate restored source reopen | — | 7.60 s |
| Candidate cold service restart | — | 13.15 s |

The candidate's repeat preparation message reached the browser about 108 ms after the click and preceded the matching acknowledgement. Actual 390 px screenshots show the loading text within the viewport and readable; the completed outcome/latency charts remain readable at phone and desktop sizes. Two axe/reflow scans passed.

The first browser receipt includes a **discarded 77 ms baseline repeat timing**: its old mounted controls satisfied the first harness readiness condition before that new read completed. That number is not a speed result. The original receipt remains intact. A focused continuation performs an actual catalog reopen, proves the old model controls are absent, then measures the repeat at 15.82 s. The corrected reusable harness is `browser-corrected.mjs`; it was prepared after the original run and is not mislabelled as that run's executed source.

## Executed evidence

Evidence below is external to Git. The joined receipt identifies every underlying receipt and its SHA-256.

| Receipt | Scope |
| --- | --- |
| `candidate-check-03/results.json` | 17 checks: exact models, every CSV byte, complete JSON/provenance, SVG and rendered scientific HTML; changed nested value; current report/study/run authority; same-revision catalog corruption; changed complete journal; equal-sized artifact corruption; all original/copy tables, objects and jobs unchanged. Both normal candidate reads replay all 789 events. |
| `state-check-01/results.json` | 14 Shiny checks over an actual copied store: pending before work, wrong/stale/duplicate acknowledgement, source changed while waiting, actual full read, numerical page 8, navigation, authority revoked before acknowledgement and explicit recovery. Client acknowledgements are supplied by the test; browser paint is covered separately. |
| `paint-results.json` | Seven fake-DOM queue tests: two complete animation frames, duplicate/replaced/removed ticket, hidden document, unrendered output, duplicate script loading. |
| `legacy-regression.log` | Existing 42 task-plot and Shiny checks pass with only acknowledgement steps adapted. Includes five ordinary task profiles, native replay, known/unknown timing, missing evidence, person-specific cohort support, exact exports and revocation. |
| `C:/Users/User/Documents/Brohn QA/task-plot-perf-01/results.json` | 16 full-app browser checks, two axe scans: exact original 384-row JSON/CSV/outcome SVG equality, visible pending before matching acknowledgement, ready, warm download refusal after study-authority loss, restored reopen and cold restart; zero new jobs/errors. The invalid baseline repeat timing is excluded as explained above. |
| `C:/Users/User/Documents/Brohn QA/task-plot-perf-repeat-01/results.json` | Two focused checks and corrected repeated-source timing after real close/reopen; originals and source files unchanged. |
| `visual-review.json` | Independent manual inspection of the actual phone pending state, phone original outcome chart and desktop latency chart. No assistive-technology usability or scientific-validity claim. |
| `natural-viewport-results.json` | Independent inspection of seven original pre-scroll MutationObserver records: the entire loading status was inside the viewport before matching acknowledgement. This proves those tested positions; arbitrary scroll positions are qualified in the later task viewport continuation. |
| `portable-js-results.json` / `portable-state-01/results.json` | Seven self-contained Node handshake checks and 14 self-contained Shiny checks using a newly generated 48-trial synthetic receiver fixture. The latter performs one full original-source read with scoring trapped after setup and no job or table changes. These exercise the first merged loading implementation, before the focus/viewport follow-up. |

Joined receipt: `joined-results.json`, SHA-256 `13f7abb31f8da50221d793f6543cb998a7363f91af8e072c5864656dc8c3dd62`.

Actual browser receipt: `ca69ab7eabb58efe584664b4f8350cbee77ac24855a9da4b8114902e6d504296`.

Focused corrected repeat receipt: `dea41ce15fece42847b8e76e0d7ac91cfafc17278efa6030a65e959758fd6d8e`.

Natural viewport inspection receipt: `8152f5300fc16ceefbf30a3ec388ce21d0695ff9a5f1f43d439d49ad37405715`. The observation records precede the harness's later `scrollIntoViewIfNeeded()` call used only for screenshot capture. The seven original click-to-message times range from 30.7 to 107.7 ms. Self-contained Shiny receipt: `ca99ebe8782a598b9cf70ad99bb8c8479ce4f756e55153f7501a3a7049d95933`.

All owned R/browser processes are terminal and port 3967 is closed. The closure inspector verified every copied application source against the initial production/candidate manifests as well as the unchanged production files. Full file manifests also preserve the unrelated, already frozen media optimization in this baseline; the source phase is not falsely equated with clean main alone.

## Dependency and integration audit

`platform-load.R` loads `signal-values` before `task-evidence` and `task-plots`, regardless of `ui=TRUE/FALSE`. Researcher session CSV/registry exports enter through `platform-run-review-views.R` in that loaded closure. All tracked evidence invocations in direct tests and researcher fixtures start with `brohn_load`; none source evidence alone without its helper.

Both `scripts/run-participant.R` and `scripts/run-worker.R` call `brohn_load(ui=FALSE)`. `scripts/analysis-worker.R` independently sources `platform-signal-values.R`; it currently neither sources nor invokes task evidence/plots. The isolated base-environment source in the GNAT plot test invokes only the pure label helper, which has no digest dependency. No missing-helper fallback or global hashing change is needed for the tracked routes.

Merge only the five files in `candidate.patch`: three existing R modules, `www/task-plot-ui.js`, and the adapted `tests/platform-task-plots.R`. Existing app installation/signatures are unchanged; the task explorer entry includes its scoped script. No loader, worker, scoring, schema or CSS edit is required. Patch SHA-256: `00f3be6d3a61240fd496b90ac03f4fd5e84ab7b9f79e31df94a027413d451836`.

## Reproduction and retained attempts

Run R scripts from the repository root with the restored R library and `LC_ALL=C`. `check-candidate.R` and `check-state.R` require the original terminal GNAT002 workspace at the explicit external path in those scripts. They copy it into a fresh argument directory and never mutate that original. Browser scripts require restored R, the publication native helper, installed Chrome, Playwright and axe; their configured paths are explicit. `browser.mjs` is the original executed harness, `browser-repeat.mjs` is the executed focused continuation, and `browser-corrected.mjs` incorporates the future repeat-timing correction. These source-dependent acceptance scripts are not self-contained default QA-catalog tests. In particular, the timing comparison must start from the pinned pre-optimization baseline, not the now-merged application: rerunning both phases against the merged code would not reproduce the before/after comparison. The adapted existing `tests/platform-task-plots.R` remains the ordinary self-contained regression.

Retain `evidence-01/02` profiling harness failures, `evidence-03` successful original profile, `hash-01` per-value hash diagnostic, `candidate-check-01` wrong event-count assertion, and `candidate-check-02` mutation setup blocked by the original SQLite immutability trigger. The final corruption test deliberately drops that trigger only inside a copied-store rollback transaction, then validates exact restoration. None of these intermediate failures was rewritten as an uninterrupted successful run.

## Current viewport and keyboard continuation

The original trace proves visibility at its seven tested scroll positions. A later two-file UI refinement makes the guarantee explicit for a request button near the bottom of the phone: focus and instantly reveal the current pending status, require its full viewport intersection before the two-frame acknowledgement, and hand focus to the exact resulting evidence/plot-controls heading. Current tickets are preserved through success or failure. Deliberate keyboard focus changes, subsequent blur, trusted wheel/touch scrolling, superseded tickets and navigation prevent stale completion focus. Access and complete-source checks remain unchanged.

The parent integrated the final task R/JS alongside a separate media focus change before this follow-up. Final task view SHA-256 is `c60ffc7b5f8636722027561e4f187a4de884bcbaa91733d17729ccb142c521a4`; final task JS is `4d20b921c0f1fece3d0cc3501db459466d058a686bd417b4dcdaaf3bc3f9ccc9`. This source phase is distinct from the earlier speed comparison. Complete manifests for both phases remain retained.

The actual integrated-source Chrome continuation passed **13 checks and one clean 390 px axe/reflow scan** at `C:/Users/User/Documents/Brohn QA/task-plot-viewport-02/results.json`. It positioned real buttons 12 px above the bottom edge, used trusted clicks, and recorded focused status bounds at acknowledgement without any harness scroll of that status. Exact next-Tab order, manual keyboard/wheel yielding, a deliberately delayed acknowledgement after navigation, and focused current-authority refusal passed. The original 384-row JSON/CSV/outcome SVG exports are byte-identical to the previous baseline. All original/copied tables, objects, jobs and application files remain unchanged; zero scientific jobs or browser errors. Actual pending, ready and refusal screenshots were manually inspected and are readable.

| Current-source route | Click to visible pending | Click to ready |
| --- | ---: | ---: |
| Saved source choices | 32.7 ms | 0.916 s |
| First complete original source | 34.5 ms | 11.567 s |
| Source with manual keyboard movement | 130.3 ms | 7.876 s |
| Source with deliberate wheel scrolling | 128.7 ms | 7.800 s |

These remain scoped observations, not a concurrency or capacity claim. The first viewport attempt stopped before requesting plots because its exact button-position gate had not been established. It is retained at `task-plot-viewport-01/failure.json`; the corrected harness waits for settled DOM geometry and keeps the same gate. Its exact initial layout cause was not captured, and no product edit was used to bypass the failure.

The final **19-check R/Shiny** and **19-check Node** tests are prepared for `tests/platform-task-plot-state.R` and `tests/task-plot-ui.mjs`. They ran against integrated source and require no retained QA store. R creates one synthetic 48-trial receiver fixture; Node uses only built-ins. Both accept optional result locations and otherwise use fresh temporary directories. Detailed invocation instructions are in external `work/task-plot-viewport-20260925/portable/README.md`.

Current browser receipt SHA-256: `720682934fea081b90d8651c2bf96adcbfa1bd33bb4e12e157b9a057ddf4da6c`. Joined continuation receipt: external `work/task-plot-viewport-20260925/joined-results.json`, SHA-256 `36893387729073fdc71d1a401fe149cc12aafa856633a01aeea190f5204958ea`. All owned processes are terminal; port 3967 is closed.

Published component invocation instructions: [task preparation checks](../../tests/TASK-PLOT-REPRODUCE.md). The external source-dependent comparison harnesses remain distinct from these portable tests.

The publication audit found that the supplied R test still sourced the external
candidate view after loading the application. The installed test removes only
that redundant external path and uses `brohn_load(ui=TRUE)` exclusively. Its
19 checks pass against installed production in
`work/media-feedback-next-20260925/installed-task-state-01/results.json`; the
installed Node test separately passes 19 checks in `installed-task-paint.json`
in that same external folder. The originally supplied file and receipt remain
retained. This is a test-portability repair, not another production or browser
source phase, and it created no supervised scientific work.
