# Integrated local checkpoint — 8 September 2026

This records executed component and process checks, not complete platform or
scientific qualification. Evidence directories are outside the repository under
`../../work/test-runs/`. Each catalog run retains its exact implementation hashes,
test hashes, logs and `results.json`. Subsequent scoped fixes below are separate
source snapshots; the broad checkpoint is not relabelled as a later build.

Latest scoped additions: [native task import](NATIVE-TASK-IMPORT-RESEARCHER-JOURNEY.md)
passes 19 browser checks/five accessibility scans with one intake and two saved
analyses; [session/task review](SESSION-TASK-REVIEW.md) passes 25/seven.
Questionnaire answer revision passes 75 pure R, 28 actual receiver/direct-scoring,
14 Shiny, 39 pure JS and 23 actual browser/receiver checks/two scans, plus 13
legacy browser checks. Core 109, delivery 56 and offline HTML ten regressions
pass after the shared hooks. The full researcher/scientific-report journey is
running; it does not cover the separately identified large-history transport
boundary. Task cohorts pass 68 pure, 36 storage and 31 Shiny checks; actual
saved-worker and browser acceptance are in progress.

| Retained catalog run | Outcome |
| --- | --- |
| `brohn-integrated-domain-20260908-v1` | 23 of 23 selected test files passed. |
| `brohn-integrated-scientific-20260908-v1` | 8 of 9 passed; neural test closure omitted the required publication module. |
| `brohn-integrated-neural-20260908-v2` | Corrected fixture: 44 contract, disclosure and actual-worker checks passed. |
| `brohn-integrated-operations-20260908-v1` | 15 of 16 passed; acquisition completion/quality assertion failed. |
| `brohn-integrated-acquisition-20260908-v2` | Diagnostic rerun passed 43 checks without a product change. This alone does not resolve the original failure. |
| `brohn-integrated-interchange-20260908-v1` | 4 of 4 selected test files passed. |

The acquisition investigation independently completed eight real recording/stop/
archive cycles and a Windows exclusive lock on `status.json`: 58 checks retained
under `acq-9548769720ac`. The lock makes optional live telemetry unavailable while
the completed catalog revision, original hash and final manifest remain unchanged.
The original assertion depended on this optional telemetry. This is a reproduced
weakness, not proof of what occurred during the undiagnosed broad failure.
Durable terminal-quality fields and the corresponding review are now corrected.
A separate original probe also exposed a 266-character Windows chunk path failure;
its incomplete original and diagnostics remain retained. The targeted fix passes
48 R,21 Python and75 retained actual-process/path checks: shorter owned chunk
names, full checksum preservation and preflight refusal of unsupported paths.
See [acquisition investigation](ACQUISITION-COMPLETION-INVESTIGATION.md).

Later scoped deltas: initial capture failures now truthfully distinguish absent
incoming bytes (38 ingestion lifecycle checks; 12 actual Shiny/history checks),
peripheral Collect cards describe the enabled calibrated import (14 route checks),
and readable plot labels plus resolved automatic support settings have 24
actual-worker synthesis/report checks. A browser-reproduced logical-NA blank
numeric input is accepted as automatic at 0.1 Hz; malformed explicit durations
still fail. The full retained-source browser journey now passes48 assertions and
eight clear accessibility scans, including the0.1Hz retake, five exact saved
reports, combined liking, exports and reopen. See
[peripheral researcher journey](PERIPHERAL-RESEARCHER-JOURNEY.md).

The full MaxDiff browser import regression exercises the explicit completed-file
review and background intake: 27 assertions and five clear accessibility scans,
four actual ingestion jobs, one valid scientific result and two intentional
scientific rejection cases. See [source import regression](SOURCE-IMPORT-REGRESSION.md).

Further connected researcher journeys and changed-source checks continue before
release acceptance. Human comprehension, named-device accuracy and externally
hosted recruitment retain their own acceptance evidence.

The next protocol slice separately passes56 versioned option-assignment checks
with an independent Python oracle and actual release/API/reuse, plus affected
core109, delivery56, portability90 and scales74 regressions. Its researcher draft
controls pass12 actual Shiny checks; original assigned-protocol review/download
passes16 stored-byte/selection checks. The complete browser journey passes32
assertions/six clear scans, including eight completed sessions and32 exact native
responses. The next section slice has66 pure-domain and20 actual compiler,
nested clone/ZIP, receiver and saved-worker checks; the connected editor passes34
actual Shiny/CAS checks in the initial editor. A rapid browser name/Create race
led to atomic form capture; the editor now passes37 checks. The final section
journey passes18 browser assertions/four scans, two actual completed participant
sessions and two saved-worker reports. See [section acceptance](QUESTION-SECTIONS-RESEARCHER-JOURNEY.md).

RT support now has56 full-receiver/oracle and324 existing method checks, including
162 upstream IAT references. Twelve presentation checks cover per-metric reasons,
exact rate denominators, partial badges, offline reports and unchanged historical
rendering. This changes the named RT scoring support recipe; it does not rewrite
old reports or alter non-RT method arithmetic.

Task-score exports additionally pass15 checks, including mixed studies and
separate task/metric eligibility. Source-bound imports pass113 pure checks with
five frozen original CSV/registry pairs; full native journal/export/reimport15
and mapping Shiny14 checks pass. Saved-worker/browser acceptance is still active.
Session history now has33 metadata catalog and11 actual Shiny pagination checks,
plus16 original protocol-review regressions. These are later source changes, not
a retroactive claim that the earlier full suite ran against them.

Saved-report presentation separately passes22 scoped,10 existing offline HTML and
17 actual-browser checks/five accessibility scans. It reads the same five saved
peripheral reports; no worker or new scientific report was used. All original
JSON/CSV bytes, source/report hashes and job histories are unchanged. See
[report comprehension](SAVED-REPORT-COMPREHENSION.md).
