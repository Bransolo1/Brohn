# Historical sessions and partial task-result review

The fresh actual researcher-browser journey passes **25 assertions and seven
accessibility scans**. It runs its own Shiny service and launches no scientific
subprocess. All owned processes exit cleanly.

The catalog contains 42 deliberately uncompleted original fixture starts, plus
one unrelated study start. Their explicit seeded timestamps define the expected
newest-40 and oldest-two pages independently. They are not observed people or
completed research sessions. The first start freezes a required camera policy;
later starts use the edited draft without camera recording. No browser camera or
recording bytes are involved.

Actual Collect and archived Review retain both pages, exact order and the old
assigned protocol. Stale offsets and another study's page command are refused.
Both old-protocol downloads match the stored SHA-256 and full decoded content,
including the former required camera policy. Archiving preserves access.

The mixed report is a separate presentation fixture. An original 48-trial choice
RT journal passes the strict inner-task R receiver before direct scoring: eight
correct practice trials, one correct 500 ms test response and 39 observed test
timeouts. The report wrapper supplies the task title and identity fields that
the production job adapter normally adds. Its provenance explicitly distinguishes
this direct synthetic replay from the uncompleted catalog starts. No physical
timing, human response or production-worker execution is claimed here.

The researcher UI and standalone HTML show partial support, 39 of 40 omissions,
zero errors among one answered trial, available 500 ms mean/median and unavailable
sample SD requiring two retained responses. The dedicated task CSV contains all
five metrics even though this report also contains an explicit liking observation.
It preserves participant text `001`, collection origin `sample`, material origin
`synthetic`, `task_eligible=true` and `metric_eligible=false` for unavailable SD.
The unavailable value stays empty; support retains divisor zero and the exact
39/40 and 0/1 populations. The general observations CSV remains the separate
one-row liking source. JSON and the saved report envelope remain exact after
archived-study navigation, export and reopening.

Final evidence:
`../../work/test-runs/brohn-session-task-review-6GXiNg/evidence/results.json`,
SHA-256 `31c71cd42dc272fe2a87b94042d612f886f7a9aa498116232757815c6ba45375`.
Desktop and 390-pixel scans cover session lists, archived review, the task report
and standalone HTML. These automated checks do not establish observed human
comprehension.

Earlier evidence remains under `brohn-session-task-review-8I8jJJ`. Those runs
exposed test assumptions, not product fixes: the error belongs in the alert,
Review nests its sessions card, restored-study rendering needs an explicit
checkpoint, and a closing modal must disappear before an accessibility scan.
The initial direct-score fixture also omitted the job adapter's task title,
creating a heading-order failure. The final fresh fixture includes that required
presentation field and passes the entire journey; no old report was rewritten
to obtain a passing result.

Harness: [researcher-session-task-review.mjs](../../tests/researcher-session-task-review.mjs).
Original fixture: [researcher-session-task-review.R](../../tests/fixtures/researcher-session-task-review.R).
