# Questionnaire answer review: researcher acceptance

The connected journey passes **19 assertions and five accessibility scans**,
including a retained-source reuse continuation. One actual participant completes
four sealed questionnaire occurrences and one automatic scientific analysis.
All owned services stop cleanly. These results apply to this ordinary-size
original fixture; large accepted editing histories are a separate boundary.

The original study contains two short text packaging concepts, seven questions
before them, one question after each concept, and a three-question end assessment.
The 100 ms stimuli only test software boundaries; they are not a research timing
recommendation. The questions and two-item mean are original synthetic arithmetic
fixtures, not an established psychological instrument or observed participant data.

The researcher must explicitly enable answer review from Questions. Returning
to forward-only and re-enabling it must create deliberate saved revisions;
stale commands must be refused. A participant starting the earlier released
design must still receive its unchanged forward-only protocol.

| Actual interaction | Independent expected outcome |
| --- | --- |
| Submit native `false`, answer its direct and transitive follow-ups, then change to numeric `0` | The false-only answers disappear from final data. A separate `answered(driver)` follow-up stays visible but loses its previous answer. An independent answer keeps its original event identity. |
| Change back to `false` | The formerly hidden questions return blank; their old answers do not reappear. |
| Finally choose numeric `0` and review | The committed value is numeric zero. Hidden answers cannot appear in review or ordinary final observations. |
| Skip an optional text response and continue past information | Final records distinguish explicit omission from information acknowledgement. Neither becomes an invented numeric zero. |
| Seal the before assessment and each after-stimulus assessment | Back and Edit stay inside the current occurrence. A completed earlier assessment is not offered for editing across the next stimulus. |
| Submit end-scale values `2,4`, edit the first to `6`, reload while revisiting the second, and confirm `4` | One final scale assessment equals `(6+4)/2 = 5`. The first item has one correction and no claimed initial response latency; the unchanged second item preserves its original answer head. |
| Automatic report and full JSON | Twelve final status records, nine ordinary answered/omission observations and one end-scale score. Full history retains the driver sequence `[false,0,false,0]` and exact original event identities. |
| Template, clone, portable import and reopen | The opt-in policy and typed dependencies survive under fresh design/question/scale IDs; the original release, completed journal and report remain immutable. |

The intended actual run has four sealed questionnaire occurrences: before,
after the first concept, after the second concept and end. A separate legacy
browser start remains deliberately uncompleted and must not produce a report.
Only the completed reviewed run should launch one analysis job.

The broader lost-ACK, local-storage-failure and server-state pagination tests are
owned by the independent participant integration workstream. This journey adds
the connected researcher workflow and an actual untimed reload; it does not
duplicate or substitute for those transport tests.

Harness: [researcher-question-revision.mjs](../../tests/researcher-question-revision.mjs).
Fixture: [researcher-question-revision.R](../../tests/fixtures/researcher-question-revision.R).


## Recorded execution and limits

The actual browser verifies the researcher toggle, stale-command rejection,
Back, typed branching, dependent clearing, explicit review, untimed reload and
all four seals. The report contains exactly twelve final status records and
nine ordinary answered/omitted observations. Its complete revision projection
matches the original receiver projection, and every history event matches its
original event ID and content. The sole end-scale assessment equals five.
The ordinary observations omit hidden questions and information acknowledgements.

The first run passed fifteen checks and four accessibility scans, including the
participant completion, scientific report and exact JSON/scale CSV. Its later
protocol-list step enumerated the asynchronous session buttons too early. The
harness now waits for the selected run's real button. A retained continuation
checks the exact assigned-protocol bytes, template/clone/portable import and
reopening without repeating the participant or scientific job. The first run's
failure record remains intact; no source journal or report was regenerated.

Final evidence:
`../../work/test-runs/brohn-question-revision-ui-UGrjMV/reuse-retake-1788862851261/results.json`,
SHA-256 `91f59b29a45b668e1c530dd0b1c214b25eceef8b43cb32cc58f70dc73e3843c5`.
The original `evidence/scientific-checkpoint.json` records all 38 executed source
hashes, native publication identity and exact report envelope. Its SHA-256 is
`ee0de4b4719edf940fb782549c23a59574b49aac76cb10735101175d0c45b38a`.
The reuse continuation is [researcher-question-revision-reuse.mjs](../../tests/researcher-question-revision-reuse.mjs).

Visual review found one presentation gap after those passing checks: the before
assessment's review screen still says `Part 2 of 19`, reflecting the receiver's
held occurrence cursor instead of the displayed review step. This was reported
to the runner owner for a display-only correction. The cursor must remain held
until sealing; changing it to make the label advance would violate the contract.
No human comprehension or physical timing qualification is implied by these
synthetic browser checks.
