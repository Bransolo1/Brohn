# Saved reports: results-first acceptance

Status: accepted for the connected saved-report presentation scope on 25 September 2026. Evidence is explicitly joined across two source phases, not an invented uninterrupted run. No new scientific job, report calculation, study or participant session was created. Original source workspaces, sealed objects, report bodies and inherited jobs remain unchanged.

## What changed

In-app reports lead with Results, with compact native links to Explore and Evidence. All explorer outputs and original control identities stay mounted. Download report remains the single primary action; the other exports and exact saved-at/immutable metadata live in Evidence.

GNAT shows its two saved deadline-specific contrasts and direction explanation before expandable original protocol/context/outcome evidence. Audio shows up to eight original saved recording measurements, preserving order, units and recording/segment/channel context; unavailable or ambiguous support cannot expose a numeric summary. Its complete original feature table remains available. These presentation choices do not alter calculations, pool recordings or imply emotion or calibrated loudness. Combined reports retain their original effect cards and unavailable-comparison explanations.

Default standalone HTML and existing exact JSON/CSV exports remain unchanged. Full report titles are retained, including the real long combined-study title.

## Joined actual browser evidence

Evidence root: `C:/Users/User/Documents/Brohn QA/`.

| Receipt | Source phase | Actual completed evidence |
| --- | --- | --- |
| `brohn-results-first-06/failure.json` | Data view `c097bcbdfc2e683d5932ec0122172a4df60ab5a24a207165aa6984857889ac5c` | 75 completed checks, 10 scans. Complete GNAT/audio navigation, exports, detailed explorer activation and cold restart; original-source revocation and restored reopen for audio. Combined desktop passed; its first mobile number was partially below the viewport. The failure remains retained. |
| `brohn-results-first-07/results.json` | Data view `8c29299ae6b5de0d2088e33ac08b8325f2d30a1e317abcb189417781126da1ef` | 59 passing focused-continuation checks, 6 scans. All three current first screens, keyboard anchors and exact downloads repeated; exact saved metadata verified in Evidence; combined route and cold restart completed. |

The continuation verifies that `R/platform-data-views.R` is the only production R/www delta from06, binds the exact retained failure receipt and checks its GNAT/audio immutable-source verification. That delta relocates the same metadata paragraph. It does not rerun their previously completed full routes or claim their dynamic-open timing was measured under the metadata-only change. The check totals include repeated assertions and should not be added as unique tests.

Receipt SHA-256:

Receipt 06 failure: `f60905ff8b1278115811347ab1dcb4f680aedc79d9a1df84399881eebc04c684`.

Receipt 07 accepted continuation: `92d1fd7ebf4a2720a5aee0d192723ed6d8ef7d0e921f0c1922ab5d1de98c92cb`.

Both phases used actual retained original reports, copied from terminal workspaces: the completed native GNAT002 report in `gnat-connected-07`, the derived-audio original-source fixture, and the combined peripheral/liking fixture. GNAT07's overall earlier run is not relabelled passed merely because it contains this completed immutable report.

No source report was reseeded or recalculated. GNAT JSON/HTML/task-score CSV and combined JSON/observation CSV were compared against retained prior exports. All actual JSON/HTML/observation CSV downloads were also compared against saved bodies and pre-browser serialization. Default export changes were separately excluded by component tests.

The browser checks cover:

- Results before tools/evidence, one primary report action, unique mounted control IDs and 15-40 px populated sibling spacing, retaining the existing empty-output gap check.
- Native Results/Explore/Evidence keyboard links focus their labelled sections. Next Tab reaches the first available control, and Back to results preserves native focus and report identity.
- GNAT complete 384-position task plots and audio review controls activate from formerly empty outputs without a new job.
- An already issued audio JSON download URL refuses three requests after deliberate copied-source project revocation. One 44 px keyboard-dismissible notification replaces duplicates; Space restores main focus. Restored copied authority allows the same original report to reopen with cleared dynamic controls.
- Actual researcher stop/restart reopens each immutable report. Original workspaces, all report bodies, sealed objects and inherited jobs remain unchanged; zero new jobs and zero browser errors.
- All 266 R/www fingerprints match within the accepted 07 run. Post-stop source observations for04/05/06 are retained separately and are not described as continuous monitoring.

## Physical display and accessibility

The final 07 screenshots were manually inspected in addition to axe and geometry checks. All six 1440 px/390 px scans had zero axe violations and no horizontal page overflow. The strict first-value detector excludes descendants of closed disclosures and checks actual browser visibility.

On 390x844, final values use 28.8 px text:

| Saved result | First value | Vertical bounds |
| --- | --- | --- |
| GNAT 750 ms target-positive contrast |1.2816 |701.06-745.69px |
| Acoustic RMS |0.21579087 FS |715.70-760.33px |
| Combined temperature contrast |+8 degrees C |765.38-810.00px |

The actual long audio and combined titles were retained. All first values, labels and section links are legible and fully visible; the audio digital-scale explanation is visible too. `brohn-results-first-07/visual-review.json` pins the six inspected viewport image hashes. Full-height captures are retained, but manual inspection of every pixel of those long images is not claimed. This scope does not establish all report families, devices or assistive technologies.

## Measured responsiveness still needs improvement

`brohn-results-first-06/readiness-timings.json` records actual monotonic durations with a 30 s diagnostic ceiling:

- GNAT catalog identity acknowledgement: 3.20 s; complete 384-trial source after pressing Show complete saved source: 15.10 s; total 18.31 s.
- Audio first control preparation: 6.33 s; restored-source reopen: 1.73 s.

Controls succeeded without an error. The workspace status remained Saved report ready for review. These are observed local durations, not a benchmark or fast-load claim. Clearer progress and source-open latency remain follow-up work; no source cache/authority bypass was introduced to improve a timing number.

## Retained failures and component checks

-01/02 stopped at harness navigation locators; neither establishes the unvisited journey. -03 correctly failed the GNAT first-phone-value gate at y1302-1347 despite Results preceding tools. -04 passed 19 checks, then used a 5 s source-control wait without first acknowledging the exact bound source identity. This timeout alone is not proof of a product-free load. The later acknowledged 30 s path succeeded, and 06 measured its actual cost.

-05 completed 31 GNAT checks and 4 audio layout checks, then exposed acoustic features hidden in a closed disclosure. Its original rectangle-only visibility detector could also see hidden table descendants; the detector was tightened rather than excusing the screen. -06 exposed the long combined title clipping its value at y824.50-869.13. Each failed receipt and screenshot remains unchanged. Every created copied fixture passed post-failure original/source/job verification; services were stopped before product changes.

External component receipts remain in `work/report-results-first-20260924`:

- Initial results/section structure: 22 R and 60 DOM checks plus 2 native-anchor handler checks.
- `task-presentation-01`: 24 R and 14 independent DOM checks for compact GNAT, eligibility guards and unchanged originals/default exports.
- `audio-first/evidence-01`: 28 R and 9 independent DOM checks for exact saved measurement presentation, source support/refusal edges, zero/missing distinctions, bounded rows and unchanged default/full tables/exports.

## Re-running the fixture

This is an opt-in retained-fixture browser check, not a default fresh-install test. It requires terminal synthetic reference workspaces and their named retained exports; these research workspaces and evidence images are not Git fixtures. The source modules must be frozen, ports free and the output directory fresh and separate from the project and all original sources.

The packaged `tests/researcher-report-results-first.mjs` and matching `tests/fixtures/researcher-report-results-first.R` remove the developer's hard-coded runtime/reference locations. The harness takes one JSON request file with explicit absolute project/output/reference/R/Python/native-manifest/Chrome paths. Its syntax and R parse were checked; the accepted browser receipts above used the recorded external harness versions. Runtime-configuration packaging alone is not a new browser acceptance run.

Example request (replace all paths with actual prepared prerequisites):

```json
{
  "schema": "brohn-report-results-first-request/1.0",
  "project": "C:/work/Brohn",
  "output": "C:/qa/brohn-results-first-NEXT",
  "port": 3961,
  "references": {
    "gnat": "C:/qa/retained-gnat",
    "audio": "C:/qa/retained-derived-audio",
    "combined": "C:/qa/retained-combined"
  },
  "runtime": {
    "rscript": "C:/tools/R/bin/Rscript.exe",
    "r_library": "C:/tools/brohn-r-library",
    "publication_python": "C:/tools/brohn-methods/Scripts/python.exe",
    "publication_native_manifest": "C:/tools/brohn-native/publication-guard.json",
    "chrome": "C:/Program Files/Google/Chrome/Application/chrome.exe"
  }
}
```

Run the configured Node executable with `tests/researcher-report-results-first.mjs <absolute-request.json>`. The project needs its Playwright/axe dependencies. The accepted fixture used researcher port 3961 and no participant receiver; the packaged request specifies the researcher port explicitly. `continue_from` is optional and deliberately restricted to the retained 06 metadata-relocation continuation contract; omit it for a complete fresh presentation traversal.

No whole-platform completion, new method validation, hardware synchronization, hosted deployment, unlimited report size or production performance signoff is implied.
