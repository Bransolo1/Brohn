# Complete questionnaire evidence in the researcher workflow

Status: **passed on 2026-09-08: 10 connected browser acceptance assertions, seven automated accessibility scans and one actual synthesis worker**. The retained receiver-boundary report and its separate large-input storage proof had already passed the storage agent's 27 checks before this journey began.

The journey uses the actual researcher application, Chrome, saved SQLite records, immutable object downloads and one later real synthesis worker. It does not use mocked browser endpoints or physical devices. The accepted-run source is a newly recreated original receiver fixture: the earlier temporary boundary workspace no longer exists. This is a new qualification run, not a retry or restoration of that vanished workspace.

## Independent source expectations

The large source has 200 long-text questions answered twice. For question `boundary-q-i`, the first version is 19,000 `a` characters followed by ` 1 i`; the final version ends ` 2 i`. The full result must preserve all 200 final answers and all 400 acknowledged answer versions. Its complete questionnaire history has 803 events: 402 visits, 400 commits and one seal. The whole run journal has 804 events including the final run ending. Each final record has one revision and no claimed uninterrupted initial response time. This boundary contains no omitted or hidden questions; those distinctions have separate connected answer-review evidence.

The browser fixture also creates a separate, explicitly labelled original arithmetic source. Its first 12 observations are nonnumeric context text. Rows 13–18 contain three numeric condition pairs: 1→3, 2→6 and 3→9. The ten-row response preview therefore contains no numeric outcome. The actual Combine measures workflow must read the complete artifact, expose the quantitative question, retain all six exact source-row identities and obtain an equal-person mean difference of 4 from three people and three visits. This constructed source report is not misrepresented as an earlier scientific-worker publication; the subsequent synthesis is the actual child-process check.

## Acceptance checks

- Open the retained report through Studies → Results. Full observation, summary, final-record and acknowledged-event counts are visible. Each limited table states its preview count; long text is explicitly abbreviated.
- Download complete JSON and observations CSV through the real UI. Reconcile all 200 unabridged final values and 400 answer versions against the independent text formula and original retained commit hashes. Decode each CSV `response_record_json` field and require exact equality with its full typed observation, including null values and source identities.
- Download the typed analysis artifact. Check its full-file SHA-256 and byte count, bounded line sizes, ordered typed nodes, full-record counts and source binding. A separate JavaScript decoder must reconstruct the same complete analysis as the JSON export.
- Export standalone HTML. Keep its preview/full-count distinction clear and check desktop and 390-pixel rendering and automated accessibility.
- Reopen in a fresh researcher browser session. The full JSON, study, saved report envelope, source event hashes and artifact bytes remain unchanged.
- Select the constructed source in Combine measures, explicitly review identity linkage and declare the numeric condition comparison. One actual synthesis worker must use all six numeric rows beyond the preview and preserve original report/row provenance.
- No browser exceptions, visible Shiny errors or automated accessibility violations remain in the exercised routes. Automated accessibility checks supplement, rather than establish, human usability qualification.

## Executable files

- `tests/researcher-questionnaire-artifacts.mjs`
- `tests/fixtures/researcher-questionnaire-artifacts.R`

The harness takes the qualified retained workspace path and boundary report ID. It starts only an owned Shiny service and later launches one explicitly queued synthesis worker. It does not start a participant service or rewrite the original boundary study. Product source changes and execution are coordinated with the parent to preserve scientific code identities.

## Limits

Hydration and export still use bounded in-memory analysis structures. This journey qualifies its recorded fixture and complete-source synthesis, not unlimited memory, arbitrary cohort sizes, human participants or hardware timing. Failed states and later retakes remain separate; evidence is not overwritten to make an earlier attempt appear successful.

## Recorded execution

Evidence is retained at `work/test-runs/brohn-questionnaire-artifact-ui-TKsPwT/evidence-1788865953753` under the task root. `results.json` has SHA-256 `8be8b9d9a12d3052ce532871af0355d0807c07e3278ddf2dbe38420459319c4c`; `scientific-checkpoint.json` has SHA-256 `6efc096032be8a908f945f3131394d552620dded2c055b58b9b813e40fdd4786`.

The original saved boundary report is `report-job_46fd985e3f6b341cc11c16fd7cd217d7`, with unchanged report hash `b36aca2fd61598fc05a0fa7385bba94d871571d5f933c548405892eac61d1c63`. Actual browser downloads retained:

| Download | Bytes | SHA-256 |
| --- | ---: | --- |
| Complete JSON with provenance | 24,110,326 | `4aeb82394ae011fb29bc68d66d2b68758774d6f6c5b75f82f5ef52823ad68ee4` |
| Complete observations CSV | 7,871,502 | `b87bea561f06c9c2c9bee6e496775f739a1f113499cd612af393d65535639c03` |
| Original typed analysis artifact | 24,405,317 | `81d3b340b1471680f9952272bf8b3003144f32e846a37644bf4af7e417b783ea` |

The independent JavaScript typed-node reader reproduced the complete JSON analysis exactly. All 200 final values matched original commit text hashes, all 400 committed versions and 803 questionnaire history events survived, and every revised response time remained null. A fresh researcher browser session downloaded byte-identical full JSON. Desktop and narrow screenshots were inspected visually in addition to the automated scans; download controls remained readable and the report clearly distinguished 200 full records from ten displayed records.

The separately constructed source `report-artifact-oracle-c0081fcc7c760308c70f61b35c33a0fe` contained its only six numeric observations after the preview. The actual UI discovered that outcome, accepted explicit participant-linkage evidence and queued `job_ca87cf641e12de09c4cd0691bd9f3e67`. Its saved report `report-job_ca87cf641e12de09c4cd0691bd9f3e67` preserved source rows 13–18 and their original report hash, obtained mean difference 4 across three independent fictional people and three visits, and retained Holm family size one. The report carries 41 executed source-file identities and native publication build evidence. No queued or running jobs remained in the retained workspace, and the owned Shiny service stopped normally.

The first browser attempt failed only because the test requested an exact table-region name without the shared horizontal-scroll accessibility suffix. The harness selector was corrected; the same source/report was reused, with no repeated scientific job or product modification. That failed attempt remains in the preceding evidence directory. This acceptance found no product defect; it does not establish human comprehension or physical-device qualification.
