# Integrated workflow continuation — 24 September 2026

The owner reconfirmed sustained end-to-end implementation after the source
publication checkpoint. This record tracks the subsequent connected work;
publishing a checkpoint does not complete the 50-capability, 17-package plan.

The owner subsequently paused development and requested publication to `main`.
See the [workstream](../WORKSTREAM.md) for ordered continuation after a resume.

| Workflow | Executed scope | Evidence |
| --- | --- | --- |
| Original-permission camera automation | 13 loaded integration checks, 17 joined browser groups including seven clean scans, six independent native-reference checks. Five successful jobs include exactly one platform facial inference; original source, complete artifacts and report survive restart. Synthetic camera feed and model-accuracy limits remain explicit. | [Camera automation](CAMERA-FACIAL-AUTOMATION-ACCEPTANCE.md) |
| Preserved participant delivery and recovery | 57 direct preservation/27 dispatcher checks; 74 actual browser/receiver checks and three scans. Separate final-runner fault injection passes 20 checks/three scans for failed local storage, lost acknowledgement and admitted-session recovery without a duplicate start. | [Runtime preservation](PARTICIPANT-RUNTIME-BROWSER-ACCEPTANCE.md) |
| Researcher code provenance | 30 domain/Shiny, seven connected/four scans, 21 independent checks; final narrow wrapping/keyboard correction adds two browser/two scans and four independent checks. Exact release/run/report/History links, original manifests, no added jobs during review. | [Code provenance](PARTICIPANT-CODE-PROVENANCE-ACCEPTANCE.md) |
| Preferred participant upload batches | 31 helper and 31 browser checks, two narrow scans; original uncertain bodies preserved, all 394 events and 15,000 key observations retained. Per-request headroom improves; total retained-history workload remains a separate limit. | [Batching](PARTICIPANT-PREFERRED-BATCH-ACCEPTANCE.md) |
| Brohn response-window SC-IAT | Four full192-trial actual browser administrations plus a separately interrupted refresh; native/declared-summary exports, reviewed two-person cohort, restart and102 independent raw-key/Fraction checks.13 successful jobs with no original rescoring. Joined13 prior+5 finish browser groups and12+3 scans; final global legend clarification separately passes14 exact render/cross-profile checks and2 actual browser layout checks. | [Researcher acceptance](SCIAT-WINDOW-RESEARCHER-ACCEPTANCE.md) |
| Optional facial AU/native-category processing |12 contract,9 independent native,34 domain,5 actual R adapter and7 source-guard checks;16 connected browser groups including5 scans and4 successful jobs. Exact decimal selection, missing/multiple faces, complete exports and restart preserve originals. Runtime setup separately passes5 helper groups and29 checked-configuration assertions. | [Facial processing](FACIAL-EXPRESSION-ACCEPTANCE.md), [installation](FACIAL-RUNTIME-ACCEPTANCE.md) |
| Faster canonical transport |174 exact-byte/hash/error/locale/fuzz checks;74 storage,56 delivery and24 numeric-precision regressions. Same3.12MiB canonical envelope encodes38.05→3.06s, request validation3.62s. 13 full HTTP/restart checks retain15,000 key observations; first large append14.03s has only0.97s headroom against the current15s timeout. | [Exact-byte performance](CANONICAL-ENCODER-PERFORMANCE.md) |
| Close and retain a collection | 19 domain checks; ten grouped researcher/participant checks; four accessibility scans. Actual analysis retry, completion/withdrawal, immutable inventory, historical clone, archive and service restart. | [Collection history](COLLECTION-HISTORY-JOURNEY.md) |
| Complete response and scale distributions | 29 focused, 14 worker and 11 browser checks; six scans. Independent audit of eight CSVs/120 rows and nine SVGs against complete saved sources. | [Distributions](EXPLICIT-DISTRIBUTIONS-ACCEPTANCE.md) |
| Responsive complete-value review | Nine focused and 27 existing checks; 11 browser checks. Recorded ECG navigation, exact original-report preservation and fresh authority checks. | [Responsiveness](SIGNAL-REVIEW-RESPONSIVENESS.md) |
| One-command local setup | 19 setup/recovery checks; ten browser/service checks and two scans. Fresh separate R library, native build, failed preparation/retry, actual app/service launch and study persistence. | [Local setup](LOCAL-SETUP-ACCEPTANCE.md) |
| Paired-person comparison review | 32 domain checks; 28 connected browser checks/five scans; 13 independent export checks. Complete 1,000-person source, background cancellation/navigation, aligned figure/table pages and service restart. | [Paired results](PAIRED-RESULTS-ACCEPTANCE.md) |
| Faster paired aggregation | 15 ordinary/18 benchmark checks, existing46 analysis/31 scale checks. Four actual saved workers preserve full analysis/provenance; independent post-job verification passed. Direct1,000-person scorer191.23→2.36s; full worker24.70s. | [Performance evidence and retained harness failure](PAIRED-AGGREGATION-PERFORMANCE.md) |
| Linked signal and event review | 13 worker, 13 authority and 11 grouped browser checks; five scans. Exact timestamps/full CSV, missing samples/events, common cursor/window, stale URL refusal, actual cancel/retry, paging and restart. | [Linked review](LINKED-REVIEW-ACCEPTANCE.md) |
| Abandoned-session recovery | 40 domain, eight browser journey and19 independent checks; seven clean accessibility/layout scans. Live late-upload rejection retains browser data; one actual confirmed-completion worker, finalized collections, restart and backup/restore preserve originals. | [Session recovery](SESSION-RESOLUTION-ACCEPTANCE.md) |
| Source waveform and spectrogram |34 worker and28 domain checks;21 browser checks/four clean scans plus separate exact-restart and live SVG label-bound receipts. Original acoustic reports retained; cold reopening8.23s. | [Audio review](AUDIO-SOURCE-REVIEW-ACCEPTANCE.md) |
| Saved event-EDA review |27 R,11 independent reader,22 browser and38 independent source/export/figure checks; six clean inspected scans. Seven actual import/scientific/review jobs succeed; restart creates no new jobs. | [EDA review](EDA-EVENT-REVIEW-ACCEPTANCE.md) |
| Exact answer to session evidence |30 domain,6 connected browser and32 independent source/export checks; five current scans. Original30 events and report preserved; seven explicitly accounted jobs, with zero jobs during final reopening. | [Answer/session acceptance](ANSWER-SESSION-ACCEPTANCE.md) |
| Derived video/audio access across review |21 processed-signal and14 interval checks, including commit rollback and both interval-reuse ends;8 actual HTTP/browser checks and two scans for general reports and complete WAV downloads. No new browser jobs or changed original reports. | [Signal/interval authority](DERIVED-AUDIO-SIGNAL-AUTHORIZATION.md), [actual downloads](DERIVED-AUDIO-DOWNLOAD-ACCESS.md) |
| Compact understandable saved reports |24 saved-support checks; six current browser checks/four clean scans plus existing22 comprehension/10 HTML checks. Plain-language coverage with full details, compact populated cards and one44-pixel keyboard-dismissible error message. Earlier five-check layout receipt stays separate. | [Report layout and coverage](REPORT-LAYOUT-AND-NOTIFICATIONS.md) |
| Protected isolated-team setup |31 direct policy,26 wired storage/participant-handler,13 actual Shiny dispatcher checks;12 joined browser groups/five scans across actual OIDC, collection/report/history/restart, revocation and30-second leases. Explicit source-transition receipts retain original failures. | [Acceptance and deployment boundaries](HOSTED-PROFILE-ACCEPTANCE.md) |
| Video audio preparation |32 worker,26 original domain,22 browser checks/four clean scans and seven actual-worker publication-revocation checks. Imported stream selection and consented fake-camera capture, full exports, mapping, acoustic analysis, cancellation/retry and restart preserve originals. Later pre-save curation correction passes29 domain checks. | [Extraction acceptance](VIDEO-AUDIO-EXTRACTION-ACCEPTANCE.md) |
| Saved respiration cycle/phase review |16 reader,27 domain,21 uninterrupted connected checks/five clean scans. Seven actual jobs include original import/scoring and five source-bound reviews. All36,000 source samples, declared negative-volume polarity, full cycles, gap/edge/unavailable/paging/export/restart retain original reports. | [Respiration acceptance](RESPIRATION-REVIEW-ACCEPTANCE.md) |
| Saved EMG input, envelope and burst review |13 reader,31 domain and24 connected checks/six scans across11 successful jobs. Complete converted input, cleaned voltage, RMS and configured bursts retain exact source/export/restart identities. A separate current-source check covers12 respiration/EMG history and axis assertions/two scans with zero new jobs. | [EMG acceptance](EMG-REVIEW-ACCEPTANCE.md), [respiration acceptance](RESPIRATION-REVIEW-ACCEPTANCE.md) |
| Whole-recording EDA review |14 native,35 domain and21 connected browser checks/five scans; seven successful import/analysis/review jobs. Exact clean/tonic/phasic samples and nullable candidate markers survive restart. An actual5,000-candidate publication plus11 background-verification and4 fresh-authority checks establish scoped responsiveness and refusal behavior. | [Continuous EDA acceptance](EDA-CONTINUOUS-REVIEW-ACCEPTANCE.md) |
| Windows artifact paths |All27 existing artifact tests in their required environments plus one long-directory regression; saved table values and full object identities unchanged. | [Path correction](PHYSIOLOGY-ARTIFACT-PATHS.md) |

The scientific worker error adapter now exposes its actual structured failure
before checking success-only modality fields. Four checks through the real
Python duplicate-clock failure preserve the original source and return its
precise reason. Evidence: `make/work/test-runs/brohn-worker-errors-20260924-04/results.json`.
The first three fixture attempts stopped before launching Python because the
fixture omitted required source notes and the analysis-input schema; those
attempts are retained and do not count as worker failures. The existing46
independent research-analysis assertions also pass after this integration.

Actual video-audio mapping exposed a blank-number boundary: Shiny sends an empty
numeric control as logical `NA`. The mapping filter now omits missing scalars
while retaining typed zero and false; seven checks use the installed Shiny number
handler. Existing40 questionnaire explorer,12 Shiny intake and10 offline HTML
checks also pass. Extraction and respiration subsequently passed their connected
gates above; exact answer-session navigation subsequently passed its separately
recorded source, visible-table, export and reopening checks.

Source guards now span child reads for the new source-bound review operations,
generic signal catalogs/previews and derived-audio analysis. Six checks through
an actual `audio_tracks` child establish native write-handle refusal while
processing, successful read/publication, unchanged bytes and release afterward:
`make/work/test-runs/brohn-processing-guards-20260924-02/results.json`.
The first attempt correctly processed the job but its release assertion failed
because the fixture retained the ordinary read-only file attribute. Attempt02
clears only that isolated fixture attribute and proves writable-before,
denied-during, writable-after without modifying source bytes. It does not claim
that all operation branches were independently exercised.

The pre-save derived-audio mapping correction passes29 domain checks in
`brohn-audio-extraction-domain-20260924-04/results.json`: invalid study relinking
and revoked parent ownership leave the previous mapping/revision unchanged;
legitimate channel metadata still saves. Four focused Python artifact tests
cover complete new EMG input (10,000 exact mV-to-uV samples), unchanged features
and burst events, the six existing modality adapters, partial EMG support and
short cardiac input. Connected EMG review subsequently passed its separately
recorded whole journey and current-source axis/history checks above.

The core suite also passed 109 assertions after the initial shared integration.
The current shared source subsequently passed109 core and30 actual supervised
job/publication checks in
`make/work/test-runs/brohn-shared-integration-20260924-01/results.json`.
The receipt pins the exercised shared files and confirms they stayed unchanged;
it does not cover every independent module being developed alongside them.
Later integration receives its own required checks; this is not a whole-catalog
or final release result. The [completion backlog](../PRODUCT-COMPLETION-BACKLOG.md)
and [remaining work](../KNOWN-GAPS.md) retain the full product requirements.

The actual journeys found and corrected Unicode CSV transliteration, blocking
paired-source preparation, repeated large-report hashing, PowerShell configuration
replacement and multistream scratch-path growth. Their retained failures belong
to their respective evidence records. Wider input bounds, human comprehension,
physical-device timing and remote deployment remain separate qualifications.
