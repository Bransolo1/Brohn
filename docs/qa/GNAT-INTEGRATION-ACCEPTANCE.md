# GNAT shared integration evidence

24–25 September 2026. The adopted [Brohn procedure](../methods/GNAT-BROHN-PROCEDURE.md)
is connected as the seventh ordinary authoring choice; the six previously
enabled definitions are retained. These shared checks complement the separate
[accepted researcher journey](GNAT-RESEARCHER-ACCEPTANCE.md), including its two
complete administrations, native/imported reports, cohort and restart. The
published `d2889f4` baseline predates this GNAT checkpoint.

## Declared-summary and report contract

GNAT uses the explicit `brohn-gnat-trial-summary/1.0` adapter. Its frozen phase,
round, pairing cell and Go/No-Go action accompany hit, miss, false alarm and
correct-rejection outcomes. Accepted responses use Space and an actual latency;
withholding has no key or time. Correct rejection has true completed accuracy.
Interruption has no completed accuracy, and an unpresented row cannot become an
observed trial. No final-correct response is invented for interchange.

The import form derives its fields from the selected original task and
preselects matching native-export column names. The researcher still declares
the source's latency and completion definitions. Unknown definitions retain
every original value, count and descriptive rate, with no eligible metrics,
sensitivity or criterion. A known-onset response at the exclusive deadline is
refused. Summary imports never acquire original key/visibility history or native
journal evidence merely because their numbers agree.

`tests/platform-gnat-import.R` passes **24** current integration checks in
`work/test-runs/gnat-import-20260924-02/results.json`. This includes real CSV
serialization/readback, complete 384-row coordinator/scorer agreement,
source-preserving unknown timing, missing/unpresented evidence, frozen-field
substitution refusals, exact cohort schema/units, cloning and form distinctions.
The first attempt stopped after five successful groups because the fixture
selected a training correct rejection while expecting a test-scoring
disposition. The fixture now explicitly selects a test row; no product rule was
changed to satisfy that assertion. The original CSV/registry are retained.

The existing five-profile summary suite passes **114** assertions after the
adapter changes. Existing IAT Shiny selection/attachment/authority checks pass
**15**, including the actual task-dependent rendered response controls.

The pasteable category editor passes **13** focused checks, covering the 2–64
exemplar bounds, stable existing material identities, unchanged reopening,
atomic moves between categories and rejection of ambiguous duplicates. Evidence:
`work/test-runs/gnat-authoring-20260924-01.json`.

After normal seven-profile registration, direct regressions pass **330** task
method checks, including all **162/162** upstream IAT reference tasks; **24**
collection-route checks; **57** release-runtime preservation checks with all
19 current assets (including both GNAT scripts); and **30** material/reuse checks.
Existing native evidence and cohort suites also pass **15** and **68** checks.
These are component/regression results, separate from actual browser acceptance.

The final shared tree also passes **109** core assertions through the ordinary
isolated runner in 1.86 seconds:
`work/test-runs/gnat-shared-core-20260924-01/results.json`. The receipt retains
the current source inventory and unchanged-source check. It does not replace
the separately configured seven-test qualification or the connected GNAT run.

Independent review found that imported reports saved `definitions_known`, while
the plot read `definition_known`. The plot model now preserves the original
declaration and supplies the display flag, leaving original rows, scores and
hashes unchanged. Its **25** focused cases cover known/unknown GNAT and legacy
imports plus the native model; see [core acceptance](GNAT-CORE-ACCEPTANCE.md).
The Overview form now also retains the selected GNAT measure, verified against
the actual loaded UI. The subsequent connected run uses both corrections.

## Actual R receiver and durable workspace

`tests/platform-gnat-integration.R` passes **38** groups in
`work/test-runs/gni-01/results.json`, against source-pinned GNAT core
`33e6dda5c793628d33e70e2efa11ca213efd685493895fb014e70fc05f4893b8`.
Two original authored administrations each deliver 786 outer receipts through
the actual receiver, including all 384 trials and 12 instruction boundaries.

The checks cover consent/equipment/session authority, contiguous persistence,
full native journal replay, every actual CSV cell and frozen registry,
native/imported metric/count/audit agreement, ten separate cohort outcomes for
two explicitly linked synthetic people, clone/template reuse, portable ZIP and
exact protocol/journal/export reopening after the store is closed and reopened.
All exercised sources stayed unchanged during the run.

Each completion queued exactly one automatic analysis request. This component
fixture explicitly cancelled both before claim; it does **not** claim browser
execution or successful background report publication. The connected researcher
journey must exercise those boundaries separately.

## Remaining activation work

Real browser serialization exposed fractional-clock arithmetic that integer
authored receipts did not exercise. Its focused R/JavaScript correction and
source transition receive separate evidence; the preceding integer integration
receipt is not relabelled as a run of later source.

Before activation, finish the full participant journal, authoring and original
task assignment, automatic reporting, native export and actual import,
four-outcome/latency plots, reviewed cohort, accessible numerical/figure exports
and service restart. Preserve strict half-open deadlines and original evidence
through focus loss, held/repeat keys, late dispatch and failed durable writes.
No software fixture establishes physical timing, material validity or construct
interpretation. The full platform scope remains unfinished.
