# Saved interval acceptance — 20 September 2026

The actual researcher report explorer now creates, edits, removes, reviews and
restores source-bound interval sets. Automatic background calculations use the
complete immutable processed source and publish a separately preserved result.
Cancelled or failed exact-input work can start a new attempt. Earlier summaries
remain labelled with their original annotation version after an edit.

Executed evidence:

- Python worker:12 checks, including independently known arithmetic, adjacent
  half-open boundaries, overlap, exclusion/missing distinctions, unsupported or
  substituted source identities,1001 full samples and real exclusive-output CLI.
- Actual R/Python/native publication route:21 checks, including means4/10,
  sample SD2,3+3 samples, historical access, cancellation/publication fencing,
  original-byte preservation and store reopen. Evidence:
  `oka/work/brohn-signal-intervals-e048125e3f55`.
- Shiny component:15 checks covering stale controls, invalid bounds, escaped
  labels, versioned removal/restoration, exact measure selection, real catalog
  publication, cancelled retry and source clearing on navigation. Evidence:
  `oka/work/brohn-intervals-component-01`.
- Actual Chrome + researcher app + supervised worker:13 checks with3 clear
  axe/page-overflow/44px-target scans; real SVG/CSV/JSON downloads, immutable
  earlier result after edits, stale action rejection, review/restore, cancellation
  and retry, fresh-browser reopen. Evidence:
  `oka/work/brohn-intervals-browser-02/browser-1789877862622`.

The first browser attempt calculated/exported correctly, then its screenshot
helper targeted a Shiny `display:contents` wrapper. Targeting the visible chart
region corrected the test. Source evidence and failed-run output are retained.
Subsequent responsive plot presentation is checked separately against the saved
result in `oka/work/brohn-maxdiff-plots-browser-02/browser`: desktop and320-viewBox mobile charts retain both means and all axis labels without horizontal panning. All5 combined plot scans are clear.

All inputs are original numerical fixtures, explicitly marked sample. This is
software and arithmetic evidence, not physical-sensor or physiological validity.
The wider replay/annotation requirement remains open; see
[method boundaries](../methods/SIGNAL-INTERVALS.md).
