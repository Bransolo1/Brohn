# Brohn workstream and restart checklist

**Paused by the owner on 24 September 2026.** The requested wrap-up publishes
the current source to `Bransolo1/Brohn`, branch `main`. Do not restart development
or background services merely because this repository is opened. A subsequent
explicit development request can resume the relevant work.

The platform is unfinished. The [master architecture](MASTER-ARCHITECTURE.md),
[50-capability register](preparation/capability-register.json) and
[17-package manifest](preparation/build-manifest.json) retain the full scope.
This file orders the immediate continuation; it does not replace that scope.

## Current checkpoint

| Workstream | Delivered and checked | Remaining work |
| --- | --- | --- |
| Response-window SC-IAT | Six-profile task integration; four full administrations, native and imported summaries, reviewed two-person cohort, exports and restart; 102 independent source/scoring checks, 13 successful jobs. | Other implicit/cognitive procedures remain separate named profiles. Physical timing and construct qualification are not established. |
| Optional facial processing | Pinned local AU/native-category profile, explicit permission and settings, guarded jobs, complete exports, reopened reports and optional setup checks. | Frame-by-frame and temporal review, broader reference/accuracy evidence and other webcam methods. Model categories do not establish feelings or attention. |
| Automatic camera analysis | Original named camera-processing permission, exact settings and guarded automatic analysis connected to saved recordings; 17 joined browser groups including seven scans, six independent native-reference checks and five successful jobs with exactly one platform facial inference. | Broader camera/model accuracy and stimulus alignment remain unqualified. The reference browser recording uses a declared synthetic camera feed. |
| Participant delivery | Smaller new upload batches preserve uncertain operations; pinned release code, run assignments, recovery after lost receipts, restart and backup/restore. | Accumulated-history performance and concurrent workloads; preserving browser files is not a complete server/scientific environment archive. |
| Code provenance | Collect, assigned protocols, historical collections and native reports expose the same preserved identity and exact downloads; legacy history remains explicitly unknown. | Future server/compiler compatibility and reproducible release packaging. |
| Installation and operations | Checked Windows setup, exact separate R library, optional profile configuration and scoped local hosted acceptance. | Fresh-clone portable end-to-end smoke test, clean-machine installation, public hosting operations and broader roles. |

Authoritative evidence: [checkpoint](qa/PUBLICATION-SCIAT-FACIAL-20260924.md),
[workflow ledger](qa/INTEGRATED-WORKFLOWS-20260924.md),
[camera automation](qa/CAMERA-FACIAL-AUTOMATION-ACCEPTANCE.md),
[participant runtime](qa/PARTICIPANT-RUNTIME-BROWSER-ACCEPTANCE.md) and
[code provenance](qa/PARTICIPANT-CODE-PROVENANCE-ACCEPTANCE.md).
Each record preserves failed attempts and separates component, browser and
independent reference evidence. Do not add their counts into a whole-product claim.

## Next work, after an explicit resume

1. **Reproducible contributor verification (PC14/PC16).** Create one bounded
   core end-to-end smoke profile from an arbitrary checkout with explicitly
   configured external R/Python/native paths. It should create a study, release
   it, complete the actual participant flow, save one automatic report, export
   and reopen it after restart. Remove development-folder assumptions from this
   selected route before expanding the full catalog. No new smoke implementation
   was started before this pause. Reuse saved installation configuration in a
   dedicated QA launcher; the existing runtime-preservation fixture supplies a
   fresh store and actual HTTP participant route, but deliberately cancels
   analysis and therefore does not yet test automatic report publication.
   `npm run test:browser` remains the historical prototype suite.
2. **Facial result review (PC05).** Add a source-bound per-frame/temporal explorer
   over the complete saved native observations and exact original video frames.
   Preserve missing/multiple-face support, rational timing, original results,
   permission checks and complete numerical exports. Viewing must not rerun
   inference. This follow-on was proposed, not implemented.
3. **Linked media review (PC12).** Start with an existing acoustic report derived
   from one saved video. Reuse its original integer PTS/time-base and audio
   sample-zero/frame ledger to show exact video frames beside the saved waveform.
   Expose actual frame duration, gaps, quantization and mapping residuals. Pin
   parent video, extraction, report and review identities; export the mapping
   ledger and unchanged samples. Browser/restart acceptance is required. This
   investigation was read-only; no new module exists yet. External EEG/EDA/media
   alignment needs a separately reviewed map; timestamp proximity is insufficient.
4. **Continue broader completion gates.** Reconcile enabled measurement views,
   method/reference coverage, research operations, workload bounds and remaining
   registered capabilities against [PC01–PC16](PRODUCT-COMPLETION-BACKLOG.md),
   [known gaps](KNOWN-GAPS.md) and each owning package. Keep hardware-unverified,
   public-deployment and human-usability evidence distinct from software checks.

## Resume safely

- Start from current GitHub `main`; read [John's handoff](HANDOFF-JOHN.md) and
  [installation instructions](operations/LOCAL-INSTALLATION.md).
- Keep source, runtime installation and research workspace in separate locations.
  The repository excludes participant stores, models, native binaries and large
  local QA receipts. Do not point a test harness at a real research workspace.
- Preserve existing protocols, reports, manifests, original media and receipt
  hashes. Scope new work to dedicated modules; agree shared hooks before parallel
  edits and keep source stable during browser/worker acceptance.
- Use a new external evidence directory, retain failures, then record exact
  source versions, actual jobs, complete exports and restart behavior. A passing
  component check does not close its researcher journey.
- Update this workstream, STATUS, the sprint, architecture/manifest and relevant
  acceptance record when a scope changes. Publication does not complete the goal.
