# Full platform build — Brohn continuation

Resumed by the owner on 20 September 2026 after the public source handoff.
The current connected slice is recorded in [explorer acceptance](../qa/QUESTIONNAIRE-EXPLORER-ACCEPTANCE.md).
The broader unchecked roadmap remains open; this is not a full release claim.

20 September full-build continuation (scoped evidence, no competitor sign-off):

- [x] Primary iMotions release/video benchmark and measurement academic audit.
- [x] Guided Home/Overview and read-only participant sequence preview.
- [x] Optional welcome content and PNG upload/replace/preview/reuse with frozen releases.
- [x] Manager-owned guarded original/reviewed acquisition publication and recovery.
- [x] Truthful15-family source/subscription/receipt/committed-sample observations.
- [x] Versioned recording intervals, recovery, exact full-data summaries and exports.
- [x] MaxDiff choice/utility plots, denominator/missingness evidence and SVG/data export.
- [x] Saved checked installation configuration; clean-machine release still open.
- [x] Respiration quantity/polarity correction and conservative explicit Morlet baseline support.
- [x] Longer live waveform previews and selected measurement-specific preflight criteria.
- [x] Reusable equipment setups with fresh exact source/criteria review for each recording.
- [ ] Native participant camera/input checks that distinguish capture, receipt and task readiness.
- [x] Pinned material upload/preview/description/replacement across existing passive and IAT/BIAT/AAT routes.
- [ ] Optional questionnaire illustrations and image choice items with frozen asset identity.
- [x] Explicit cross-recording interval reuse, exact boundaries and target-data-only summaries.
- [x] Complete task/person, cardiac-spectrum and full time-frequency visual journeys.
- [x] Run the first preselected annotated ECG reference subset; preserve failed agreement.
- [x] Evaluate a frozen offline ECG candidate on declared diagnostic and held-out records; retain the no-promotion outcome.
- [x] Execute a predeclared annotated PPG reference comparison without substituting ECG timing for pulse peaks; retain the artifact/timing limitations.
- [ ] Source-bound cardiac artifact review and recalculation without bridging excluded spans.
- [ ] Resolve cardiac polarity/morphology review and broader independent detector validation.
- [ ] Whole-platform reference validation, synchronized replay, research operations and hosted profile.

Read [completion backlog](../PRODUCT-COMPLETION-BACKLOG.md) for acceptance and
[measurement audit](../qa/MEASUREMENT-ACADEMIC-ACCEPTANCE.md) for remaining method gaps.

Authorized 2026-09-08. Quality and complete researcher journeys take precedence
over speed. This checklist is a continuation record, not a release claim.

Current continuation: original task export/import is accepted through 19 browser
checks/five accessibility scans and two saved analyses. Answer review now has
75 pure R, 28 receiver/scoring, 14 Shiny, 39 pure JS, 23 actual browser/receiver
checks/two scans and 13 legacy browser checks. Full researcher/scientific-worker
acceptance passes 19 browser checks/five scans with one publication. Task cohorts
have 68 pure, 45 storage and 33 Shiny checks; saved-worker acceptance passes 29
checks/five publications and the browser journey passes 19 checks/five scans.

- [x] Connect opt-in answer review to frozen compilation, receiver state, durable
  browser commands and one effective answer projection for summaries/scales.
- [x] Connect descriptive task cohort membership, source integrity, explicit
  identities, repeat weighting, processing and complete export functions.
- [x] Finish the full researcher-to-saved-report journeys for both connected slices.
- [x] Replace oversized inline participant journals, full answer histories and
  final projections with verified typed artifacts and bounded manifest/report
  previews. Retain full downloadable source and exact analysis semantics.
  Connected components pass 57 input, 62 codec, 24 storage/export/synthesis and
  ten independent integrity checks. Actual large workers pass 27 checks/two
  publications; browser downloads/reopen/synthesis pass ten checks/seven scans.
- [x] Connect and qualify complete-answer storage/index, guarded worker preparation
  and researcher browsing; see the current scoped acceptance record.
- [x] Refine participant information review wording and within-part progress:
  32 actual browser/receiver checks, two clear automated accessibility scans.

Latest connected evidence: camera 61 backend checks, 39 form/download checks and
63 actual researcher/participant checks with nine clear accessibility scans;
signal views 34 browser checks/six scans; gaze plots 23 browser checks/three scans;
compound rule authoring 18 browser checks/three scans. All are scoped synthetic
software evidence. Webcam manifests now include the frozen policy and study
context, verified through the actual download. Another 16 actual-receiver checks
and one narrow accessibility scan cover Stop and Decline during pending camera
permission, plus same-page/reload recovery of a lost decline acknowledgement.
No late tracks or camera preview reappear; declined visits complete without
recording bytes or recording publications.

The exact 46-package R lock restores into a fresh separate library with all
namespaces/source/license metadata matching. Readiness has 21 checks; the actual
Windows entry point has ten. Local service recovery has 74 actual-process checks,
including shutdown during recording and subsequent reopen without duplicate
writers. Acquisition components have 43 checks, interrupted-discovery recovery 22
and the actual browser journey 36/five clear accessibility scans. Stream-to-analysis
curation has 15 Python and 43 R checks, including an actual automatically queued
segmented EEG analysis. Its UI journey passes 33 checks/five clear accessibility
scans with exact original/curated artifact downloads. Questionnaire-scale
integration passes 74 scoped checks with actual saved-worker reports and portable
designs; its browser journey passes34 checks/five clear scans. Neural ERP/Morlet/frequency-tagging views
pass 47 actual-worker/array/export checks and 66 browser checks/seven clear scans.
Planned scale comparisons have 31 scoped arithmetic, worker, report/ZIP/reopen and
Shiny checks; the actual browser extension passes26 checks/five clear scans. The separate participant
launcher regression passes eighteen real HTTP/process assertions.
The responsive signal plot recheck is complete. The new selected-suite runner
executed selected questionnaire-flow and analysis checks with independent logs
and results JSON. Whole-catalog execution remains outstanding.

Actual researcher QA exposed and corrected three integration defects: missing
scale validation in the separate participant launcher, discarded HTML report
head/styles, and transient SQLite locks classified as permanent invalid requests.
The launcher now shares the domain module closure. Offline report serialization
has ten scoped checks; scoped busy handling has twelve, plus 74 storage and 56
delivery regressions. The large-file experiment still shows excessive writer-lock
duration; staged-byte preparation and short fenced registration are the next
storage correction. The collection summary has twelve checks and now explains
actual participant capture versus separately recorded/imported measures.

Operational deadline review corrected production `processx::run` calls that had
supplied millisecond-sized values to a seconds API. Helpers now enable owned-tree
cleanup. Four actual Windows assertions stop a deliberately stalled helper and
its descendant at two seconds; no receipt is accepted. Normal acquisition 43
and installation/readiness 21 checks pass with the corrected deadlines. This
does not change participant reaction-time units, SQLite busy milliseconds or
`process$wait()` milliseconds. Large artifact staging remains separate work.

- [x] New strict design/protocol contract; preserve legacy 0.1 readers.
- [x] Parent-filtered historical study/dataset pages: 21 browser/three axe and 14
  backend checks; typed operational selection 24 plus storage/catalog/acquisition regressions.
- [x] Old-study/source-report selector journey: 16 browser checks/three clear
  scans, including open-combobox accessibility and stale mapping rejection.
- [x] Qualify native parent-owned publication handles through SQL commit (40),
  full build identity (10), required startup/doctor/worker readiness (16) and an
  actual generic 1 GiB caller with concurrent successful participant receipt (10).
- [x] Stage camera63/interchange50/curation45 plus headers29/signal32; retain
  exact dependent job identity and complete immutable JSON outside final writes.
- [x] Move researcher source ingestion onto a separate pending entity, background
  worker and staged publication:38 lifecycle,18 real dispatch,6 owner handoff,
  12 Shiny/history and10 actual512 MiB shared-dispatch checks. Complete MaxDiff
  import regression passes27 browser checks/five clear scans.
- [ ] Move manager-owned acquisition preservation and remaining legacy/direct
  import callers onto independently qualified ownership paths.
- [x] Calibrated imported temperature/acceleration:17 Python,42 R,19 independent,
  26 actual saved-job/artifact and24 reviewed synthesis/report checks.
- [x] Peripheral researcher journey:48 assertions/eight clear scans,14 settled
  jobs/five exact reports, combined liking, low-rate blank-input repair and reopen.
- [x] Correct terminal acquisition quality/history independently of optional
  telemetry; preflight paths and shorten owned chunks. R48/Python21/retained75
  checks pass. Original uninstrumented broad failure remains unattributed.
- [x] Version participant option assignment without rewriting old releases:
  56 independent/core/API/reuse and12 actual Shiny draft-command checks.
- [x] Read and download original assigned session protocols:16 exact-byte,
  study/project selection, stale/closed review and pagination checks.
- [x] Option-assignment researcher/participant/reuse journey:32 assertions/six
  clear scans, eight completed sessions and32 exact typed responses.
- [x] Section domain66 plus integrated compiler/nested clone/ZIP/receiver/worker20.
  Preserve flat protocols, scale assessments and non-question phases; correct
  the pre-existing partial-name lookup defect in nested AND/NOT design cloning.
- [x] Connect section authoring with whole-group scope/order edits, Save/Cancel,
  in-dialog errors and stale/CAS guards:37 actual Shiny checks, including atomic
  live name/target capture after a reproduced browser race.
- [x] Section researcher/participant/report/reuse:18 browser assertions/four
  clear scans; two participant sessions, two automatic reports, exact scale keys.
- [x] Study/project-filtered session metadata pages:33 catalog and11 Shiny guards;
  no whole-protocol catalog decode. Original assigned-protocol16 still passes.
- [x] Dedicated task-score CSV in mixed reports:15 checks, separate task/metric
  eligibility and collection/material origins; no unavailable administration lost.
- [x] Source-bound task import domain114, decimal interchange20, native receiver/export/reimport15 and
  mapping Shiny14 checks. Browser acceptance19/five scans with two scientific reports.
- [x] RT metric-specific support:56 receiver/oracle and324 existing method checks;
  partial-report explanations and exact denominators pass12 presentation checks.
- [x] Combined-report comprehension:22 scoped,10 offline HTML and17 browser
  assertions/five clear scans; retained source/report/export bytes unchanged.
- [x] Add source-bound existing-profile implicit imports and descriptive cohort reports.
- [x] Exact catalog/report/CSV numeric transport:24 checks, preserving legacy
  raw rows, idempotency and near-limit replay. Fresh actual reports24/four scans
  and complete imports23/five scans pass exact saved/downloaded equality.
- [x] Connected MaxDiff participant and supervised-report journeys: core 47,
  authoring 32, participant component 28/three axe, platform adapter 26 and
  actual participant delivery 42/two axe checks. Researcher authoring/reuse now
  passes27/five axe; mapped import domain47/platform40 and mapping browser14/three
  axe. Complete report/export journeys caught precision and accessibility
  defects; corrected fresh runs pass (reports24/four scans, imports23/five).
- [x] SQLite revisions, immutable source objects, transactions and fenced jobs.
- [x] Participant delivery, append-only events and immutable run assignment.
- [x] Portable design implementation with adversarial archive checks.
- [x] Premium shell and participant renderer component checks.
- [x] Independent researcher arithmetic/eligibility fixtures and QA checklist.
- [x] Connect researcher shell, participant receiver and processing supervisor.
- [x] Verify a researcher creates, releases and completes a real browser study.
- [ ] Connect data curation, scientific workers, review and reproducible reports.
- [ ] Complete method/task packs and resolve all capability dependencies.
- [ ] Complete graphical authoring, AOI review and linked multimodal analysis.
- [ ] Verify historical artifacts, archive, cloning, export/import and restore.
- [ ] Execute whole-journey and accessibility acceptance checks.
- [ ] Update dependency locks, installation and delivery documentation.

## Evidence and implementation adjustments

`tests/platform-core.R`, `tests/platform-store.R`, `tests/platform-delivery.R`,
`tests/platform-portability.R` and `tests/participant-runner.mjs` cover component
behavior. `docs/qa/RESEARCHER-QA.md` distinguishes independent arithmetic from
implementation checks and a real end-to-end researcher walkthrough.

Integration review found resumed untimed questions need a new clock segment;
interrupted completion cannot require every step; questionnaire codes must stay
typed and unambiguous; downloads need an absent archive path before Shiny transfer.

New runtime: `../../work/r-library-brohn`, separate from the legacy library.
New workspace storage is outside the repository. The default launcher now starts
the connected workspace; the old application is available through the explicit
legacy launch option.

External evidence remains for physical rigs, named provider/model claims,
authenticated public hosting and user-specified GitHub destination. These do not
block independent local implementation.

Latest connected implementation checkpoint:

- Twelve questionnaire types run through the participant service; completed
  sessions enqueue real R analysis and retain frozen JSON/HTML/CSV reports.
- Five named implicit/RT profiles compile, execute and score; actual browser
  checks include the full IAT, corrected errors and interrupted task boundaries.
- Raw gaze, eight physiology routes, three event-related EEG recipes, local
  video geometry and assisted rectangular AOIs have working workers and scoped
  fixtures. Import/mapping/report and AOI review UI integration is being tested.
- Verified folder backups execute as supervised jobs. Historical design
  revisions can be inspected, cloned and exported without copying observations.
- Browser QA independently reproduced the 10.833333 pp gaze comparison,
  three-person eligibility, active-response exclusion and immutable reuse.
  The expanded lifecycle passes 85 assertions and 16 desktop/narrow axe scans,
  including close/archive/restore, historical revision reuse and rapid edits.
  A separate AOI/task extension passes 27 assertions and seven accessibility
  scans, including real segmentation, explicit review and a 28-trial browser run.
- Multistream backend preservation passes 37 supervised import/artifact/fence
  checks. Multimodal synthesis has 45 independent integration checks and 14
  additional identity-mapping/export checks; its guided UI is now connected and
  awaiting the actual researcher walkthrough. These counts describe scoped
  software evidence, not global accessibility or scientific qualification.
- Active next integration: multistream normalization, explicitly linked
  multimodal synthesis, method/design portability, operational recovery and the
  remaining capability/whole-journey acceptance criteria. No package is marked
  verified solely because its component assertions pass.

Subsequent scoped journeys: multistream UI 38 checks/five accessibility scans;
multimodal UI 29 checks/six scans; reusable analysis-plan UI 29 checks/four scans;
task downloads six checks. A new guard now explicitly detects visible Shiny
output errors, in addition to browser exceptions. Earlier counts do not claim
that stronger guard ran before it existed. The empty/failed multistream state
is undergoing its affected retest after fixing a zero-length Shiny `req` check.
Native headers have 29 supervised R/Shiny checks and nine Python header fixtures;
EDA events have 57 R checks including a durable saved report plus 28 Python tests.
Complete physiology artifacts have a separate typed verifier. The expanded EDA
suite passes 65 checks, including actual parent publication, restart and exact
download bytes for 3,000 retained samples versus a 2,000-row display preview.
The combined native/EDA browser download journey is the next affected check.
## Signal views, questionnaire logic and local recovery checkpoint

- [x] Complete typed signal catalog/preview worker: 24 checks, full-file verification,
  explicit clock and axis semantics, preserved extrema and support fragments.
- [x] R queued view publication + accessible SVG UI: 32 checks across actual workers,
  range/extrema oracles, Shiny/downloads, immutable reports, fencing and reopen.
- [x] Compound questionnaire editor: 36 R/Shiny checks; exact typed participant
  branching: 11 browser checks across two completed runs.
- [x] Native/EDA researcher journey: 45 checks and seven desktop/narrow accessibility
  scans, including complete output and explicit unsupported event windows.
- [x] Local service unexpected-exit recovery: 51 actual-process checks.
- [ ] Compound-editor and signal-explorer actual browser walkthroughs.
- [ ] Gaze visual report connection and local LSL acquisition journey.

## Camera researcher integration checkpoint

- [x] Plan camera/microphone policy authoring with required retention text,
  bounded settings, explicit Save/Cancel and stale-draft rejection.
- [x] Sessions capture/assembly state and linked Data source; complete original
  recording, callback, decoder-frame and manifest downloads.
- [x] 39 actual Shiny/catalog checks for policy persistence, cancellation,
  revision/history/reuse, artifact ownership and participant decision display.
- [ ] Actual researcher-authored camera recording, retry/reload and complete
  artifact download browser journey. Component checks do not claim this passed.

See [the camera researcher contract](../operations/BROWSER-CAMERA-RESEARCHER-UI.md).
