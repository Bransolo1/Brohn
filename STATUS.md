# Resume here

Product name: **Brohn** (confirmed 2026-09-08). App header, browser title and
development package metadata use this name.

Updated: 2026-09-08. Prefer focused reads and reviewable integration checkpoints.
**Development paused at the owner's request.** This checkpoint is being
published as MIT-licensed source at Bransolo1/Brohn. John should start with
[the handoff](docs/HANDOFF-JOHN.md). Do not resume the full build without a new
instruction. The application is an unfinished development release.
No background work or automation is scheduled. The authoritative plan is
[MASTER-ARCHITECTURE.md](docs/MASTER-ARCHITECTURE.md); docs/planning is its research archive.

## Current checkpoint

Latest connected slices: original implicit export/import passes 19 actual browser
checks and five accessibility scans, including one background intake and two
published scientific analyses. Import component evidence is 114 domain, 20
decimal interchange, 15 native export/reimport and 14 mapping UI checks; the
earlier 46-check worker execution preceded the parser extension. Paged session,
protocol and task reporting has 25 browser checks/seven scans.

Questionnaire answer review now connects opt-in authoring, exact compilation,
receiver/recovery, browser Back/Edit/seal, final-answer summaries, scales and
retained edit history. Evidence: 75 pure R, 28 connected receiver/scoring, 14
actual Shiny, 39 pure JS and 23 actual browser/receiver checks/two scans, plus
13 legacy browser checks. The full researcher-to-worker report journey passes
19 browser checks/five accessibility scans with one actual saved analysis.
Descriptive task cohorts now connect reviewed source reports, explicit identity
mapping, repeat policy, queued analysis and exports; 68 pure, 45 storage and 33
Shiny checks pass. Worker acceptance has 29 checks/five publications, the browser
journey has 19 checks/five scans, and the named-report extension has seven checks
and one actual publication from original saved fixtures. Neither slice
is a full-platform or scientific-qualification claim.

Large-history integration is connected and accepted through scoped worker/browser checks:
small native-run input manifests preserve original protocol/journal files;
complete typed questionnaire artifacts back bounded saved-report previews,
full exports and scientific synthesis. Components pass 57 input, 62 codec,
24 storage/export/synthesis and ten independent integrity checks. The newly
retained 200-question receiver recreation reproduces the historical 24,090,330-byte
overflow exactly; the historical temporary source workspace is no longer present,
so it is not an original-job retry. The retained recreation passes 27 checks
with two actual publications. Browser acceptance passes ten checks/seven scans,
including complete exports, reopen and one synthesis using all six numeric rows
beyond the preview. See docs/operations/QUESTIONNAIRE-EVIDENCE-ARTIFACTS.md.

Stopped next slice: platform-questionnaire-index.R is unregistered with 49 pure
checks; platform-questionnaire-explorer.R is an unregistered, parse-only storage
draft with zero integration tests. Neither is loaded by the application. No UI
module or worker registration exists. See the John handoff for exact concerns.

The earlier full-build authorization produced the connected slices below.
The user's later stop instruction pauses the remaining 17-package work.
Latest integration review caught a precision split between full report JSON and
catalog scalar serialization. New writes now use the same 17-digit encoder;
24 exact numeric/legacy replay checks pass, including 525 binary64 values and
near-limit historical requests. Existing JSON/hashes are never rewritten.
Fresh researcher reports pass 24 checks/four clear accessibility scans; full
imports pass 23/five, with exact catalog-versus-envelope equality. Machine CSV
also preserves exact finite binary64 numbers. Earlier failed exact-equality
evidence remains retained; arithmetic tolerance cannot excuse serialization changes.
The connected workspace now runs independently of the old prototype: researcher
Shiny, participant HTTP service and supervised analysis workers share the durable
catalog. Browser QA has exercised create/collect/import/report/reuse with actual
saved outputs: 85 lifecycle checks and 16 desktop/narrow accessibility scans are
clear. A separate 27-check AOI/task extension has seven clear scans. Remaining
capability journeys and human usability evidence are still outstanding.
Five task profiles, sampled gaze, physiology, event-related EEG, local vision and
reviewed AOI proposals are integrated or undergoing their final UI connection.
Multistream preservation, reviewed multimodal synthesis and reusable comparison
plans now have executed browser journeys. Continuous event-related EDA and native
header inspection have supervised saved-job evidence. Complete processed artifacts
retain rows beyond display previews and pass 65 EDA integration checks. Their
native/EDA browser walkthrough passes 45 checks and seven accessibility scans.
Strengthened lifecycle/interchange runs also reject visible Shiny output errors.
Compound questionnaire rules have 36 R/Shiny checks, 11 real participant
branching checks and 18 browser authoring checks/three clear accessibility scans.
Complete processed signal views have 24 Python, 32 R and 34 browser checks/six
clear scans. Pinned gaze plots have 30 R and 23 browser checks/three clear scans.
Camera capture has 61 backend checks, 39 researcher form/download checks and a
63-check actual participant-to-geometry journey with nine clear scans, including
the standalone consent/retention and frozen study manifest download. A further
16-check actual-receiver journey verifies late-permission cancellation and lost
decline acknowledgements, with one clear narrow-screen accessibility scan.
Eight independent number/slider synthesis
checks cover typed validity and equal-person repeated-session averages.
The updated 46-package R lock restores into a fresh library; all namespaces and
package metadata match with no extras. Installation readiness has 22 scoped
checks including required publication readiness. The default launcher now selects the connected workspace and passes ten
actual Windows entry-point checks using the restored library. Three-service
recovery has 74 actual-process checks; acquisition has 43 component checks, 22
interrupted-discovery recovery checks and 36 browser checks/five clear scans.
The bridge from preserved streams into scientific datasets passes 15 Python and
43 R checks, including automatic segmented EEG analysis against independent
spectral examples. The connected stream journey passes 33 browser checks and
five accessibility scans, including complete original/curated downloads and
preparation-only versus automatic analysis. Reusable questionnaire scales pass 34 browser checks/five clear accessibility
scans, including reverse keys, omissions, imports, exports and design reuse. Responsive plots retain readable axes within a 390-pixel
viewport; full saved outputs remain unchanged. Questionnaire-scale core, portable
design, import, worker and report hooks pass 74 scoped checks, including real
saved-worker reports and portable designs.
ERP, Morlet and frequency-tagging plots have 47 actual-worker/array/export checks;
their connected browser journey passes 66 checks/seven clear accessibility scans,
including standalone narrow-screen reports and complete exact data downloads.
Planned after-stimulus scale comparisons have 31 arithmetic, worker, portable
and Shiny checks; their actual browser extension passes 26 checks/five clear scans. They preserve the
declared full testing family and equal-person repeated-assessment aggregation.
The participant launcher now loads the shared domain validator closure, correcting
scale-enabled enrollment; eighteen actual separate-process checks pass. Offline
HTML exports retain the hoisted document head and responsive styles, with ten
scoped checks. A twelve-check collection-route summary distinguishes selected
research intentions from configured participant capture and separate imports.
Transient SQLite writer contention now returns retryable 503, with twelve scoped
checks plus existing storage/delivery regressions. Large artifact publication
now uses staged preparation for generic analysis and vision; remaining publishers
are active work in docs/operations/PUBLICATION-TRANSACTIONS.md. Acquisition service
file recovery has 22 focused checks. Supervised helper deadlines now use seconds
and clean up owned descendants; four actual stalled-helper checks pass alongside
normal acquisition/readiness regressions. The selected QA-suite runner retains
logs and machine-readable results; selected questionnaire-flow and analysis
executions pass, not the whole catalog.
Historical retrieval now filters the study/dataset before pagination; its isolated
journey passes 21 browser checks/three clear scans plus 14 backend assertions.
Related operational queries and project-wide metadata selectors have 24 further
checks, including actual refusal of a second recording behind old active history.
Storage 74, catalog 10 and acquisition 43 regressions pass. The selector browser
extension passes 16 checks/three clear scans, including an open-combobox
accessibility repair. MaxDiff core 47, authoring 32 and participant component
28 checks/three clear scans pass; the connected compiler/receiver/direct-R-
analysis/HTML/CSV/ZIP adapter passes 26. Actual participant delivery passes
42 checks/two clear scans, including aborted IndexedDB writes, recovery, exact
retries and withdrawal. Researcher authoring/reuse passes 27 checks/five clear
scans, including persistent actionable release errors. Mapped import passes 47
domain checks and 40 platform checks; complete import/report journeys now pass
as recorded above. Core 109, library 18, delivery 56 and portable 90
regressions pass after these integrations.
Windows staged publication now retains parent-owned native file handles across
SQL commit, with 40 focused checks including helper death during the transaction.
Generic jobs 30, vision 30 and EDA 65 affected integration checks pass. Camera 63,
multistream 50 and stream-curation 45 now also publish through staged guards;
native headers 29 and signal views 32 pass the affected path. The actual
generic 1 GiB publication passes ten checks: 0.133-second final write transaction
and a successful participant request during preparation. Native build/readiness
has ten checks plus sixteen actual doctor/startup/worker readiness checks; missing
dependencies cannot create a workspace or claim its queued jobs. Remaining paths
include acquisition-owned preservation and legacy/direct API imports. MaxDiff mapped
imports pass40 actual worker/Shiny checks; the mapping-only browser journey
passes14/three clear scans. Background researcher intake is integrated:38 lifecycle,
18 actual dispatch,6 owner-handoff and12 Shiny/history checks pass. A512 MiB
shared-dispatch run has10 checks, a0.250-second final write and a concurrent
successful participant receipt. Completed transfers require one explicit review
before an import freezes the destination; pending imports have their own history.
Calibrated temperature/acceleration pass17 Python,42 R,19 independent arithmetic,
26 saved-job/full-artifact and24 synthesis/report checks. The researcher journey
passes48 assertions/eight clear scans, including the corrected blank support
field at0.1 Hz, combined liking and reopen. Five complete saved reports and14
settled jobs retain exact sources. The full new-intake
MaxDiff journey passes27 checks/five clear scans, including expected failure cases.
The broad checkpoint completed23 domain,8 of9 scientific,15 of16 operations and4
interchange test files. The neural fixture's missing publication module was
corrected; its44 checks now pass. Acquisition now retains terminal quality
independently of live telemetry and uses bounded chunk paths:48 R,21 Python
and75 retained process/path checks pass. The original broad failure remains
unattributed. See the
[integration checkpoint](docs/qa/INTEGRATED-CHECKPOINT-20260908.md) for scope.
Questionnaire option assignment now includes participant allocation in a named
version. Older releases keep their original order; explicit draft controls have12
Shiny checks alongside56 independent/core/API/reuse checks. Assigned session
protocol review and byte-exact JSON downloads pass16 checks. Their connected
researcher/participant journey passes32 assertions/six clear scans, including
eight completed sessions and32 exact native responses. Dependency-safe sections
have66 independent domain checks plus20 actual compiler/nested clone/ZIP/receiver/
saved-worker checks. The connected section editor passes37 actual Shiny/CAS
checks; its full browser journey passes18 assertions/four clear scans with two
actual participant sessions and two automatic saved reports. A rapid name/Create
race was corrected using an atomic live form snapshot. The section
audit also found and corrected nested AND/NOT cloning caused by R partial field
matching. RT scoring now preserves each metric's eligible population and
denominator:56 complete-receiver/oracle checks,324 existing method checks and12
report-support checks pass. Partial results are visibly labelled; historical
reports retain their original calculations. Source-bound implicit import/cohort
work now has113 pure checks across five frozen original CSV/registry fixtures;
native journal export/reimport passes15 checks, mapping UI14, and storage/worker
acceptance is active. Dedicated mixed-report task-score exports pass15 checks.
Participant history now uses40-row metadata-only study/project pages:33 catalog
and11 Shiny page/guard checks pass, with16 assigned-protocol regressions. Combined report comprehension passes22 scoped,
10 offline HTML and17 browser checks/five clear accessibility scans on the same
saved reports, with unchanged machine exports and source/report hashes.
Use [the active sprint](docs/sprints/02-full-platform.md) and
[researcher QA evidence](docs/qa/RESEARCHER-QA.md) for scoped results.
Earlier preparation-only statements below describe historical checkpoints.
The new `R/platform-*` modules implement transactional version storage, design
compilation, questionnaires, local participant delivery, design portability and
the premium researcher shell. Component checks exist; connected app, analysis,
recovery and whole researcher journeys remain active. See
[the build log](docs/sprints/02-full-platform.md). Synthetic checks are not
qualified hardware support, human usability testing or scientific validation.

**Lifecycle audit and design portability added on 2026-09-08.** The scientific
plan previously under-specified historical libraries and participant delivery.
[The lifecycle contract](docs/product/STUDY-LIFECYCLE.md) now defines storage,
study/data/design libraries, curation, deployment/enrollment, collection closure,
history, collaboration, archive and restore. [Design portability](docs/product/DESIGN-PORTABILITY.md)
adds template save, design-only cloning and `.brohn-study.zip` export/import.
The registry now has **34 planned journeys across 15 lifecycle areas** alongside
the unchanged 50-capability/13-measure-card map and 17 build packages. Application
storage and behavior are unchanged; these operations remain to implement.

**Holistic build, UX, launch and visual preparation completed on 2026-09-08.**
Start with [large-build readiness](docs/preparation/LARGE-BUILD-READINESS.md) and
the [Astra execution brief](docs/preparation/ASTRA-BUILD-BRIEF.md).

- [50 capabilities across 28 families](docs/preparation/capability-register.json)
  have owners in [17 planned build packages](docs/preparation/build-manifest.json).
  Scope includes the broader implicit/cognitive catalogue, peripheral physiology,
  fNIRS, webcam/voice/movement, questionnaires and multimodal automated analysis.
- Isolated acquisition, R/statistics, survey/task, vision/audio and compatible
  segmentation environments are installed. Seven official model artifacts live
  outside the repository with recorded hashes. **101 new reference assertions
  pass** (27 + 22 + 29 + 17 + 6), alongside the earlier 81 method assertions.
  Counts combine reference arithmetic, synthetic/replay, contract and model smoke
  checks; they are not counts of validated methods or enabled features.
- [The unified experience](docs/product/UNIFIED-EXPERIENCE.md) maps all capabilities
  to 13 shared card families and [34 planned acceptance journeys](docs/product/journey-acceptance.json).
  Low-click, comprehension and accessibility targets remain to measure in the app.
- [Introduction and launch](docs/product/INTRODUCTION-AND-LAUNCH.md) define positioning,
  sample-to-study onboarding, demonstrations, design partners and release stages.
- [The brand system](docs/brand/BRAND-SYSTEM.md) includes an original folded-B logo,
  32 icons, 43 SVG assets, Manrope typography and soft graphite/mint/lavender tokens.
  The [static preview](docs/brand/preview.html) passes 31 contrast-pair checks and
  desktop/narrow axe scans. These checks do not qualify the complete product.
- Product behavior and the app dependency lock are unchanged. The visual preview,
  reference workers and downloaded models are preparation assets. No camera,
  microphone, live rig, paid API, reset or public release was activated.

## Earlier preparation checkpoints

**Method reuse preparation completed on 2026-09-08.** Start with
[the analysis register](docs/methods/reuse/README.md) for the next scientific pack.
Standard EEG, EDA, gaze, ECG/HRV, PPG, respiration, EMG, implicit and facial routes
now have specific APIs, inputs/outputs, settings, quality rules and references.

- Installed isolated MNE 1.12.1, NeuroKit2 0.2.13, CVXOPT 1.3.2 and separate R
  gaze/IAT research libraries. App dependencies and product behavior are unchanged.
- 81 targeted benchmark assertions pass: analytic/reference arithmetic, upstream
  examples, synthetic recovery and contract probes. Two R scorers reproduce 162
  bundled IAT D1 values exactly. Evidence types are recorded; these are not live
  studies or already integrated analysis features.
- Found actionable default/API issues: IAT exclusions and correction latency,
  gaze segmentation/defaults, HRV denominators, EDA amplitude missingness,
  ignored cvxEDA kwargs and automatic ECG correction. Adoption rules document them.
- BIAT fast-trial denominator is now specified from the selected primary procedure;
  author-data reproduction and complete runner execution remain to implement.
- Benchmark scripts, source hashes, aggregate results and exact Python versions
  are repository files for review; external sample data stay under ../../work.
  See scripts/benchmarks/README.md for commands. No new app runtime is activated.

**Initial build preparation completed on 2026-09-08.** Independent UX/architecture
reviews refined the six-wave roadmap, now elaborated by the master architecture
and build manifest. Questionnaire linkage, recovery, static AOI proposals and
novice/accessibility evaluation moved earlier in the complete eye-study journey.

- Installed renv and pinned 32 R packages; fresh isolated restore and full 773-check
  R suite plus wire/report reproduction pass. Working app package versions stayed fixed.
- Installed pinned Playwright/axe/jsPsych development tools and portable Quarto;
  six desktop/narrow browser checks and a standalone HTML render pass.
- New accessibility checks found/fixed missing title, language and invalid download
  icon attributes. Scanned Library/Review states now report no axe violations;
  full manual/accessibility and participant qualification remain open.
- Managed Chromium download timed out; tests used isolated installed Chrome.
  Tool inventory, versions, sources, setup and remaining candidates: docs/preparation/TOOLING.md.
- No reset redemption, schedule or full product build started in preparation.

**01A–01L delivered as bounded technical increments. The full original Sprint 1
scientific, hardware and human-usability gate remains open.**

- Local R/Shiny library and Plan, Questions, Collect, Review, Results workflow.
- Portable study/session/clock/stream/event/liking contracts and local JSON saves
  with stale-save protection. Encoders preserve accepted portable integers with
  17 digits; raw timestamps remain decimal strings.
- PNGs, original sample images, manual rectangular AOIs with pointer/keyboard
  editing, image-content bindings and revision-aware invalidation.
- Control/test designation and rationale; AB/BA or fixed-order plans, shared
  viewing duration and separate liking periods. Assignment/timing are not active.
- Unqualified prepared-interval AOI shares and paired B-A differences with explicit
  exclusions. Fictional sample Results and independent reproduction from export.
- Strict prepared CSV import in R/CLI and local UI; auto calculation/save/reopen for
  a matching study snapshot. Reports embed original CSV bytes and content hash.
- Saved protocol snapshots preserve the full study and planned trial/exposure/
  phase/order registries. Exact matches reopen; edits retain prior snapshot files.
- 773 counted R checks plus storage/report/JSON/Shiny workflow checks pass. R/JS/R transfer
  and recalculation pass, including the actual browser-downloaded reports.
- Browser verifies uploads, pointer/keyboard AOIs, save/reopen, controls, sequence
  preview, CSV import, report download and protocol save/export/reopen. The actual
  downloaded protocol validates in R. Desktop/narrow screenshots are saved.
  This is not a formal accessibility audit or undergraduate usability study.
- Native Windows R 4.6.1 / Node 24.19.0; packages and checks in DEPENDENCIES.md.
- Git main initialized and initial files staged. No author identity, commit,
  remote or public release created; no identity invented.

## Run and resume

Launch ./run-local.ps1, then http://127.0.0.1:3838. It supervises the connected
researcher, participant, analysis and acquisition profile. See
[local installation](docs/operations/LOCAL-INSTALLATION.md), DEPENDENCIES.md and
the active sprint for scoped verification commands. Isolated R is
../../work/native-r; the connected library is ../../work/r-library-brohn. The
fresh restored library is ../../work/r-library-brohn-restore. The old prototype is
available with -Legacy; tests/all.R checks that historical code, not every new
platform module. No GitHub publication has been performed.

For implementation, read the master architecture, relevant build package and
acceptance journey, then the necessary R/tests modules, docs/CONTRACTS.md and
docs/DECISIONS.md. Method notes: docs/methods/CONTROL-DESIGN.md, PREPARED-GAZE.md and
PREPARED-CSV.md and PROTOCOL-SNAPSHOTS.md. Do not reload or recreate the entire
planning/research dossier.

### Historical prototype limits and disk layout

The following two paragraphs describe `-Legacy` only. The connected platform
uses its selected workspace's SQLite catalog, versioned entities and immutable
objects, background imports and scientific workers, with method-specific limits
and historical libraries. See [the current lifecycle](docs/product/STUDY-LIFECYCLE.md).

Local UI limits: 1 MiB, 20,000 intervals, 500 participants and 40 areas, including
report reopening. Tables preview 100 rows; exports contain all rows. The CLI has
larger documented limits. Source byte bounds are not peak-memory guarantees.
Retain the local single-user scope until resource-isolated jobs are implemented.

Reports live beside each draft in a folder named <draft.json>.analyses. Latest
matching revision opens automatically and recomputes from embedded CSV. Edits
clear current output; previous reports remain on disk. No historical-analysis
browser or report-to-study import interface yet. Unreadable reports are reported.

## Continue the authorized full build

Follow the [Astra brief](docs/preparation/ASTRA-BUILD-BRIEF.md) and dependency-ordered
build manifest. Preserve the connected implementation and evidence above; do not
restart the old foundational build or treat installed engines as new features.

1. Complete the implicit import storage/worker and actual researcher journey,
   plus session-history/task-export browser acceptance. The section journey is
   complete within its documented synthetic software scope.
2. Build on corrected RT support and source-bound task imports for the explicit
   cohort contract. New SC-IAT/GNAT/priming/AMP
   profiles retain separate procedure and engine requirements.
3. Continue the remaining full capability and lifecycle packages: answer-revision
   navigation, richer AOIs, supported capture/model extensions, research
   operations and qualified deployment. Do not collapse the scope to a demo.
4. Preserve short publication transactions across remaining manager/legacy
   paths; extend cross-volume intake deliberately and bound remaining full-run
   collection/report selectors before larger studies.

Named hardware, provider outputs, human comprehension and externally hosted
recruitment retain configuration-specific evidence. No physical or human
qualification is inferred from synthetic application QA.

## Historical usage handoff

The user requested continued coding within a small allowance. The meter initially
reported 13% used (87% remaining), differing from their reported 6% remaining.
An unanswered clarification asked whether six percentage points was the intended
session allowance. The conservative working cap was 19% used. Last observed:
17% used. This coarse shared-account reading does not precisely meter the turn;
do not claim six points were exhausted or that 6% remains.
The next explicitly requested increment, 01L, also ended with a 17% used reading.
On September 8 the user requested preparation for a larger build after a reset.
The previous allowance is historical, not a cap for a newly authorized post-reset
run. No reset was redeemed or future usage reading inferred in preparation.

Update this file, the sprint checklist and CHANGELOG.md at every handoff.
