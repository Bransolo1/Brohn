# Changes

## Connected review and collection continuation — 2026-09-24

- Added complete explicit distributions, paired-person plots, linked saved
  signals/events, collection finalization and abandoned-session recovery.
- Reduced repeated large-cohort grouping and saved-value hashing while retaining
  exact results and fresh source/authority checks.
- Connected source audio waveform/spectrum review, event-EDA review and an
  isolated-team OIDC profile; their current acceptance remains recorded in the
  integrated workflow ledger while final browser work proceeds.
- Applied researcher session checks to storage, commands and downloads; separated
  hosted enrollment, material and admitted-upload policies. Expired or revoked
  participant access retains local browser data without inventing a completion.
- Shortened temporary processed-artifact filenames to prevent Windows path
  failures; complete hashes, contents, duplicate checks and object-store identity
  remain intact. The27 existing artifact tests pass in their appropriate profiles,
  alongside a215-character-directory regression.
- Added bounded saved-video audio extraction with explicit stream/channel choice,
  consented-camera lineage and complete WAV/timestamp exports; its connected
  journey passes22 browser checks/four scans and seven publication-revocation checks.
- Completed scoped audio source and event-EDA browser/export/restart evidence;
  the ledger retains separate source versions and failed attempts.
- Corrected scientific worker error handling to expose the actual failure reason
  before requiring success-only fields; four actual error-envelope checks pass.
- Completed isolated-team hosted local acceptance, including immediate cached
  data/download denial, researcher leases and separate admitted-upload expiry.
- Connected background saved-answer/session evidence;30 domain,6 connected and
  32 independent checks now pass, with five current accessibility/layout scans.
  Respiration cycle review passes21 connected checks/five scans. Corrected blank Shiny
  numeric mappings without removing legitimate zero/false values.
- Reject invalid derived-audio study relinking before saving; keep source read
  guards through child processing. Preserve EMG pre-cleaning input in new complete
  artifacts without changing features or burst detection.
- Enforced original-video authority across derived reports, exact values, plots,
  intervals and complete WAV downloads, including already-issued URLs.
- Made researcher error notifications keyboard-dismissible with a named region.
- Replaced empty report gaps with consistent populated-card spacing; repeat
  download errors update one dark notification with a44-pixel close target.
- Added plain-language saved measurement coverage and expandable complete fields,
  retaining distinct sample, event, channel and participant denominators.
- Completed saved EMG input/clean/RMS and optional burst review, including exact
  exports and reopening. Corrected distinct numerical axes and saved-window
  labels for both EMG and respiration.
- Completed continuous EDA clean/tonic/phasic review with exact candidate markers,
  exports and restart. Verify large saved results in a supervised process while
  checking current source authority before exposing values and downloads.

## Connected measurement review — 2026-09-24

- Connected the complete saved-video explorer to report navigation, exact native
  landmark tables, recorded-frame extraction and source-bound exports.
- Corrected mixed-validity timeline colors and heading hierarchy; retained open
  landmark disclosures and keyboard focus across pagination and image readiness.
- Follow replacement job identities on index/frame retry and use the existing
  Windows-safe, hash-checked download copy for immutable recorded PNGs.
- Reserve enough chart space for tick labels and units, including compact fNIRS
  optical-density views; scientific values and support remain unchanged.
- Completed exact processed-value and pupil/blink browser/export/restart checks;
  independently checked all downloaded values and the current video index resource
  boundary. See `docs/qa/PUBLICATION-CHECKPOINT-20260924.md` for scoped results.
- Reconciled the shared local checkout, architecture, remaining work and John's
  setup/handoff guidance. Brohn remains under development.

## Source checkpoint — 2026-09-21

- Included exact processed-value paging/CSV, retained pupil/blink traces and
  saved-video geometry/frame exploration components with their tests.
- Added independent fNIRS phantom and saved-output review evidence.
- Updated the GitHub handoff and documentation map for the resumed platform.
  Outstanding browser/export acceptance remains explicit in
  `docs/qa/PUBLICATION-CHECKPOINT-20260921.md`; this is not a completed release.

## Connected research workflows — 2026-09-20 (in progress)

- Added a combined participant-study journey with actual receiver events, decoded camera recording and automatic report;11 browser assertions/four scans/two jobs pass.
- Added versioned ECG/PPG source exclusions, exact time-to-sample previews,
  complete support review and new reports from separately filtered surviving
  runs, preserving original reports, bytes and acquisition crosswalks.
- Fixed processed-artifact readers retaining open files after validation,
  callback or cancellation exceptions; sources now close deterministically.
- Added native participant equipment policy, actual camera/audio previews, required
  key practice, browser/receiver recording evidence and recovery after delayed
  receipts or reload; successful transport remains separate from signal quality.
- Preserved complete pre-cleaning ECG/PPG inputs with explicit input/cleaned
  views, unchanged saved detections and source-bound exports.

- Added saved MaxDiff item images with exact best/worst choice rendering,
  recoverable preload, frozen material identity and unchanged utility scoring.
- Added optional questionnaire illustrations with exact PNG/description ownership,
  branch-aware participant review, recoverable preload and immutable release/reuse.
- Verified six original EEG files against an independent binary EDF calibration
  and explicit-DFT spectral oracle, retaining artifact/interpretation limitations.
- Added pinned image/material editing, exact previews and descriptions, replacement
  and text recovery across passive and IAT/BIAT/AAT routes, preserving frozen releases.
- Added declared interval reuse across recordings with explicit preview, exact
  boundary precision, immutable lineage and calculations using only target data.
- Added reusable equipment definitions, exact channel-container metadata comparison,
  fresh current-source approvals and immutable setup references in each recording.
- Evaluated a frozen offline ECG candidate on ten annotated recordings; retained
  production unchanged after mixed detection and interval results.
- Evaluated unchanged PPG detection against direct human pulse labels on eight
  preselected recordings; preserved artifact and endpoint disagreement despite
  complete50ms pulse matching. Reproducible reference harnesses keep raw data outside Git.
- Added guided study entry, an actual saved-state overview and read-only participant
  sequence preview; optional welcome content/image upload uses frozen release assets.
- Acquisition originals and reviewed imports now publish through manager-owned
  guarded atomic paths. Equipment views distinguish subscriptions, incoming data,
  committed recording and unqualified measurement quality across15 families.
- Added versioned signal intervals, restoration, full-data descriptive comparisons,
  exact support, and standalone graph/table/provenance exports.
- Added MaxDiff choice/utility plots and exact chart-value exports; all report CSVs
  now preserve Unicode labels/units under Windows C locale.
- Added complete task timing/distribution/person plots, source-bound cardiac
  interval spectra and complete time-frequency maps with explicit missing/edge masks.
- Completed an annotated ECG subset comparison, retaining the detector's failed
  noisy-record agreement as an unresolved accuracy finding rather than qualification.
- Added exact saved ECG/PPG detection markers, interval tables and SVG/JSON exports;
  over-limit counts explicitly retain unchecked alignment until a narrower range.
- Added five-second live signal previews and source-bound acquisition criteria;
  stale, interrupted and unqualified evidence cannot acquire a usable-signal claim.
- Corrected an import readiness race and included welcome validation in automatic
  saved-report analysis, preserving the original frozen welcome image and responses.
- Corrected respiration source quantity/polarity handling and added explicit
  frequency-dependent Morlet baseline support. Historical results stay unchanged.
- Saved local configuration checks exact runtimes before writing; failed replacements
  preserve prior setup. Historical iMotions and academic acceptance audits are saved.
- These are qualified software slices. Whole-platform completion, physical equipment
  qualification and competitor parity remain open.

## Brohn continuation — 2026-09-20

- Resumed development with the existing Brohn name and visual identity,
  preserving schemas, identifiers, installation paths and saved studies.
- Completed the source-bound questionnaire index/storage/worker connection and
  researcher explorer: Questions/Answers filtering, complete paging, native and
  structured values, unabridged UTF-8 chunks, distributions and retained changes.
  Cancel/retry and failed preparation preserve original reports and downloads.
  See [scoped acceptance](docs/qa/QUESTIONNAIRE-EXPLORER-ACCEPTANCE.md).
- Corrected the Windows identity probe, opened-handle authority checks and
  retained-source publication guards. Added explicit monitored preparation limits
  and kept immutable report controls stable during unrelated workspace refresh.
- Participant information rows use Review information; questionnaire progress
  tracks visible questions in the current part and distinguishes answer/final
  review. The actual browser/receiver suite passes 32 checks and two clear scans.

## Public source checkpoint — paused, 2026-09-08

- Paused feature development at the owner's request. Added MIT licensing,
  third-party notices, contributor guidance and John's explicit restart handoff.
  The next answer explorer remains unregistered and unfinished.

- Connected opt-in questionnaire Back/Edit and explicit review sealing. One
  receiver-derived final-answer projection feeds summaries and scales, with
  original acknowledged edits retained separately. Legacy releases keep their
  original navigation. Full report QA passes 19 browser checks/five scans with
  one saved analysis.
- Connected descriptive task cohorts with frozen source-report membership,
  reviewed person/session identities, explicit repeat weighting and separate
  outcome/evidence exports. No participant count is inferred from trial rows.
  Saved-worker acceptance passes 29 checks/five publications; browser acceptance
  passes 19 checks/five scans, including named reports and complete exports.
- Connected small native-run input manifests and complete typed questionnaire
  analysis artifacts to fenced publication, saved reports, full downloads and
  multimodal synthesis. Preview eligibility/counts/display values must match
  the complete source. Actual large-worker acceptance passes 27 checks/two
  publications; browser downloads/reopen/synthesis pass ten checks/seven scans.
- Questionnaire CSV downloads now include the exact typed original response
  record beside readable spreadsheet cells. CSV/TSV imports preserve UTF-8
  headers and answers under Windows C locale and reject malformed encodings;
  15 export and 28 reader checks pass.

- Connected source-bound implicit trial import to its reviewed original protocol
  registry, exact study version and background analysis. Native completed sessions
  export the original registry and machine trial CSV after full journal replay.
  Actual browser acceptance passes 19 checks/five scans with two saved analyses;
  imported summaries do not claim replay.
- Participant session lists now page within the selected study/project using
  bounded metadata. Full frozen protocols are read only for explicit review.
- Added a dedicated task-score CSV for mixed reports, with separate administration
  and metric eligibility, original scoring audits and unavailable results.
- Connected versioned questionnaire section/group assignment to compilation,
  portable reuse, the participant receiver and saved-worker analysis. Dependent
  follow-ups and scale items retain their required order. The section editor
  validates whole-group moves and explains rejections inside its dialog, with
  guarded Save/Cancel. Corrected nested AND/NOT rule cloning to use exact fields.
- RT results retain supported omission/error rates when correct-response support
  is sparse. Each measure has a versioned support population and denominator;
  report cards explain unavailable variability and distinguish partial results.
- Combined reports use readable measure labels and units, with specific support
  explanations. Original calculations, machine exports and hashes are preserved.
- Questionnaire option randomization now has a versioned participant assignment.
  Older releases preserve their original shared order; researchers can explicitly
  upgrade individual draft questions. Session review downloads the original
  assigned protocol, preserving exact saved JSON and typed answer codes.
- Acquisition history reads terminal quality from the checked final manifest,
  independently of optional live telemetry. Short owned chunk names and destination
  preflight correct the reproduced Windows path failure while preserving checksums.
- Corrected blank automatic support duration in the actual low-rate sensor form;
  the same retained source now analyses at0.1Hz and reopens with its resolved10s.
- Added calibrated temperature and tri-axial acceleration recipes, reviewed
  calibration/environment settings, complete signal/event artifacts, plots and
  interval-aware synthesis. Missing support never becomes a zero event count.
- Researcher imports now review the completed file and destination before a
  background job preserves the full original. Pending history, cancellation and
  explicit retry remain separate from complete scientific datasets.
- Matched catalog and machine CSV numeric precision to report/protocol JSON. Existing revisions
  keep their raw bytes and hashes; exact legacy replays remain supported without
  allowing nearby rounded values or redirected operation receipts to alias.
- Corrected MaxDiff report heading order and exercise-specific region labels,
  including expanded source/model/coverage tables.
- Generic, vision, camera, multistream, curation, header and signal outputs prepare verified bytes before the
  final database write; parent R file handles protect them through commit.
  Required Windows publication dependencies fail before service startup or job
  claims, with exact native build identity and actionable setup instructions.
- Added exact-study-version MaxDiff CSV/TSV mapping with original row evidence,
  explicit omissions and separate origin checks. Actual saved-worker, report,
  import, export and reopen journeys pass their scoped checks. Background autosave preserves actionable command errors.
- Browser progress now uses a private candidate until IndexedDB commits. A
  failed write preserves the saved cursor and prevents delivery of unsaved
  events; actual participant QA verifies one response after a successful retry.
- Added parent-filtered historical report/dataset pages and typed pre-limit
  operational queries. Older active acquisitions cannot disappear behind recent
  completed history. Study/source-report selectors search project metadata.
- Connected explicit MaxDiff designs to compilation, paired-response validation,
  direct R scoring, HTML/CSV and design-only clone/ZIP reuse. Actual participant
  and supervised saved-worker journeys pass; no conjoint or
  individual/population preference inference is implied by aggregate utilities.
- Corrected supervised tool deadlines to seconds and enabled owned process-tree
  cleanup. A deliberately stalled helper verifies bounded termination of both
  its interpreter and descendant; normal acquisition/readiness still pass.
- Added declared paired scale comparisons with exact scoring-key provenance,
  equal-person repeat-visit aggregation and the existing full hypothesis family.
  Clone/import remap scale hypotheses; plan drafts reject cancelled or stale edits.
- Shared the participant launcher's domain validators with the researcher app,
  fixing enrollment for released scale-enabled designs. Corrected standalone
  HTML report head/style serialization and responsive page containment.
- Classified bounded SQLite writer contention as retryable 503 while preserving
  invalid-request errors. Large-file publication staging remains active work.
- Added a collection-route summary derived from configured questions, tasks,
  camera policy and selected measures, distinguishing direct collection from
  separate recordings and imports.
- Fixed pending-permission camera cancellation and immutable decline retries,
  including recovery after reload. Actual receiver tests verify no late recording
  or preview and no invented recording finish after a declined decision.
- Connected preserved analogue stream curation to a segmented dataset and
  optional automatic channel analysis, retaining complete source-row decisions.
  The connected researcher journey verifies full original/curated downloads,
  automatic segmented analysis and explicit preparation-only behavior.
- Connected reusable questionnaire scale keys to study validation, cloning,
  portable designs, collected/imported scoring and report exports. Missing
  assessment identities never infer a score; saved-study QA is in progress.
- Connected camera setup, browser receipts, original recording/artifact downloads
  and optional geometry processing; researcher QA drove a portable frozen
  consent/retention/study manifest correction. Interrupted recordings remain
  explicit and cannot qualify for automatic geometry as completed sources.
- Added accessible pinned gaze and complete physiological signal views. Removed
  whole-report polling refreshes that reset active plot controls. Large processing
  tables are available through disclosures while complete exports stay unchanged.
- Updated the default Windows launcher to the connected profile and the exact
  R lock to 46 packages. Fresh restore, readiness and actual launcher checks pass.
  Child services respect the selected library. The backup CLI loads recording
  integrity extensions when checking camera-aware snapshots.
- Added one-dataset segment mapping for physiological recording resets, preserving
  source people/sessions and independent spectra/EDA processing. Numerical/sliding
  responses retain typed validity and equal-person repeat-visit aggregation.
- Added design/protocol, durable store, participant delivery and portable design
  modules, plus premium researcher and participant interfaces.
- Added researcher QA fixtures and component checks. Connected integration remains
  active; legacy readers and original research files are preserved.
- Connected real participant sessions to supervised analysis and immutable reports;
  added raw gaze, physiology, event-related EEG, five implicit/RT procedures and
  local video geometry with full preserved artifacts.
- Added graphical AOI editing and explicit review of model-suggested rectangles,
  task-aware portable designs, historical exports and supervised verified backups.
- Researcher walkthroughs drove fixes to page composition, source mapping,
  questionnaire report language, clock evidence and immutable export lineage.
- Connected typed multistream preservation and reviewed cross-source synthesis;
  actual browser journeys retain complete source clocks and independent outcome
  denominators in saved, downloadable results.
- Added reusable declarative comparison plans with executed browser/worker and
  clone/ZIP checks, native header inspection, source calibration gates and
  continuous-context event-related EDA. Full processed artifact integration is
  under the affected supervised-publication checks.
- Corrected spreadsheet export escaping, read-only Windows transfer copies,
  stale form values and numeric category-code summaries. Added visible Shiny
  output-error checks alongside browser exceptions and accessibility scans.
- Connected camera/microphone policy authoring to Sessions and immutable Data
  artifacts. Added 39 Shiny/catalog checks for draft cancellation, required
  retention text, policy reuse and safe complete recording downloads; the actual
  camera browser journey remains a separate acceptance check.

## 2026-09-08 — Historical research, participant delivery and design portability

- Audited actual local draft/report/protocol storage and documented current
  history/export limitations; no saved participant contents or locations changed.
- Added complete lifecycle diagrams and contracts for libraries, dataset curation,
  lab/hosted delivery, enrollment, closure, historical reports, reuse and restoration.
- Specified save-as-template, design-only clone and portable `.brohn-study.zip`
  export/import, with version/asset/permission validation and fresh study identity.
- Expanded acceptance from 18 to 34 planned journeys, covering 15 lifecycle areas;
  added explicit ownership and delivery requirements to the existing 17 packages.
- Updated the master, readiness audit and build handoff. No application feature,
  hosted endpoint, participant message or data migration was executed.

## 2026-09-08 — Holistic build, product experience and identity preparation

- Added the authoritative master architecture, 50-capability/28-family register
  and dependency-ordered 17-package implementation manifest; all packages remain planned.
- Installed isolated acquisition/neuroscience, platform/statistics, survey/task,
  media and compatible segmentation tooling. Recorded seven external model hashes
  and 101 new passing reference assertions; product dependencies are unchanged.
- Recorded compatibility/default findings, including the separate MediaPipe
  segmentation version and foreground encoding, and method-specific protocol profiles.
- Mapped all capabilities into 13 shared UI card families and 18 planned whole-journey
  acceptance scenarios; added introduction/onboarding/launch and Astra execution plans.
- Created the original folded-B identity, 32 icons, 43 SVGs, local licensed Manrope
  font and soft graphite design tokens. Static desktop/narrow previews pass 31
  contrast checks and axe scans; application theme integration remains planned.
- Updated working instructions and handoffs for a future connected large build.
  No new participant runner, live device, camera, paid API or product method was enabled.

## 2026-09-08 — Established methods and analysis implementation preparation

- Added a method reuse register and concrete EEG, EDA, gaze, ECG/HRV, PPG,
  respiration, EMG, implicit-scoring and facial-provider specifications.
- Installed isolated MNE/NeuroKit/CVXOPT and R method libraries; preserved app
  dependencies. Recorded 38 Python version pins, sources and rerun commands.
- Added five reference scripts and aggregate results: 81 assertions pass, including
  162 bundled IAT scores, independent EEG/HRV/IAT arithmetic and synthetic cvxEDA.
- Resolved the selected BIAT denominator and recorded reproducible package/default
  issues affecting exclusions, response timing, gaze segmentation, EDA/HRV
  denominators, ignored cvxEDA settings and automatic ECG beat correction.
- Specified the R-controlled worker/job boundary, automatic report flow and
  recipe-specific integration checks. These are preparation assets, not newly
  enabled product modules; no camera, paid API or live device was activated.

## 2026-09-08 — Brohn branding

- Adopted the user-confirmed name Brohn in the app header/browser title, project
  README, development package metadata and working instructions.

## 2026-09-08 — Preparation: reviewed build plan and development tooling

- Independent UX/architecture reviews reconciled into six integration waves;
  questionnaire linkage, recovery, static AOI proposals and novice/accessibility
  evaluation now enter the first complete eye-study journey.
- Installed renv; froze 32 R package versions and verified a fresh isolated
  restore. Added explicit snapshot/restore/namespace/version/source checks.
- Pinned Playwright/axe/jsPsych development dependencies; added six browser tests
  using isolated synthetic drafts. Installed checksum-verified portable Quarto.
- Axe found missing page title/language and invalid default download icon ARIA;
  fixed them and passed Library/Review scans at desktop and narrow sizes.
- Full 773-check R suite, wire/hash checks and report reproduction pass from the
  restored library; six browser checks and Quarto HTML smoke render pass.
- Managed Chromium download timed out; browser tests use installed Chrome
  152.0.7977.82. No camera/device SDK/model, remote publication or reset redemption.

## 2026-09-05 — Sprint 01L: saved protocol snapshots

- Review saves/exports a fixed full-study snapshot with planned trial, exposure,
  viewing/response phase and order registries. Both images are required.
- Canonical content hashes, strict regenerated-registry validation and bounded
  local storage preserve integrity; the app refuses to replace a different file.
- Matching snapshots reopen automatically. Later draft edits retain earlier
  files and clear the current snapshot indicator.
- 773 counted R checks plus storage/report/JSON/Shiny workflows pass. R/JS/R hash
  parity and actual browser save/export/reopen/download validation pass.
- Participant execution, persistent allocation, event binding and scientific
  qualification remain future work. See methods/PROTOCOL-SNAPSHOTS.md.

## 2026-09-05 — Sprints 01J/01K: prepared imports and saved analyses

- Strict canonical CSV preserves IDs and source bytes/hash; its draft analysis
  remains unqualified and separate from raw-device preprocessing.
- Portable reports embed original CSV and frozen study. Reopen/export rebuild
  derived tables; CLI and independent reproduction tools are included.
- Local UI checks/calculates/saves on CSV selection and restores matching analyses.
  Working copies separate origins; draft edits retain prior report files.
- Added UI/reopen limits, bounded table preview and visible failures.
- Fixed large-integer JSON precision and added maximum-value R/JS fixtures.
- 616 counted checks plus report/JSON/server workflows pass, alongside browser
  import/reopen/download and independent downloaded-report reproduction.

## 2026-09-05 — Sprints 01G/01H/01I: calculation and controlled design

- Added prepared gaze shares, paired differences, missingness and fictional Results.
- Added control/test designation and rationale with explicit calculation direction.
- Added AB/BA and fixed-order previews, shared viewing time and question periods.
- Recorded method requirements and UI evidence. Timing, allocation, raw device
  processing and scientific/hardware qualification remain open.

## 2026-09-05 — Sprint 01E: portable study images

- Added bounded PNG validation, content hashes and portable inline asset records.
- Added Plan upload/preview with automatic save and image-aware revision updates.
- Included two original sample designs and their reproducible R generator.
- Verified both uploads through the browser and image bytes/hashes across R/JS.

## 2026-09-05 — Sprint 01F: draft AOIs

- Added named normalized rectangles bound to exact image content, with stable IDs,
  revision-aware editing/removal and recorded-data edit restrictions.
- Added pointer drawing and percentage-field alternatives in the guided editor.
- Image replacement invalidates that image's regions; other image regions remain.
- 242 domain/storage/image/AOI checks and cross-language checks pass; drawn region
  save/reopen and refreshed keyboard entry verified in the browser.

## 2026-09-05 — Sprints 01C and 01D: local saves and guided application

- Added bounded UTF-8 draft reads, atomic replacement, locks and stale-save checks.
- Added stable study titles, revision-aware editing and preservation of imported
  question/modality metadata; evidence-bearing bundles open read-only.
- Built Shiny/bslib library and five-stage shell with liking editor, automatic
  save on navigation, JSON import/export, visible errors and sample origin.
- Added a local launcher and isolated native R installation for verification.
- Fixed a native Windows encoding bug and a browser text-input debounce race.
- Passed 109 R checks, cross-language round-trip and Shiny workflow checks;
  exercised create/edit/save/reopen through the browser.

## 2026-09-05 — Sprint 01B clock, event and response contracts

- Added validated run/clock/stream/event links and question response snapshots.
- Preserved raw timestamp precision and distinct unanswered/skipped/answered states.
- Added validated JSON encoding/decoding using jsonlite, with explicit arrays/nulls.
- Tested R → JavaScript → R round-trip, identity conflicts, stale question revisions,
  clock mismatches, invalid ratings and duplicate JSON property rejection.
- Documented dependency/runtime evidence and the next bounded handoff.
- Full Sprint 1, recording, persistence, UI and scientific qualification remain open.

## 2026-09-05 — Sprint 1 technical foundation

- Created local repository layout and low-token resumption instructions.
- Preserved the strategic documents and 238-ticket planning snapshot.
- Added paired-study, liking-question and session constructors/validation in R.
- Added planned modality registry, synthetic sample and contract checks.
- Kept hardware, scientific and formative-study acceptance gates explicitly open.
# Active full-platform integration, 2026-09-08

- Added typed compound questionnaire-flow editing and exact client/server branching.
- Added queued full-artifact signal catalogs, range views, gap-aware accessible SVG
  plots, numerical support, and immutable view/chart downloads.
- Added owned local service exit recovery with bounded retry delays and distinct logs.
- Corrected intrinsic grid sizing found in the narrow EDA report walkthrough.
- Scoped evidence: 45 native/EDA browser checks, 32 signal R integration checks,
  24 signal worker checks, and 51 actual-process recovery checks. Whole-platform
  acceptance remains in progress.
